# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M004 | arch | Workflow engine for M004 | Keep custom DB-backed step runner | Issues are implementation quality problems (hardcoded dates, UTC misuse, stub inference), not architecture problems. Temporal migration deferred indefinitely. | Yes — if step runner proves fundamentally inadequate for M005 requirements |
| D002 | M004 | arch | `/task` storage model | Workflow artifact (reuse existing machinery) | Avoids schema migration for M004. Dedicated tasks table deferred to M005 when task querying becomes a first-class need. | Yes — M005 likely needs dedicated table |
| D003 | M004 | arch | Immediate execution delivery | Inline from Telegram handler after DB persist | Operator-triggered actions execute immediately. Worker polling retained as background recovery for orphaned runnable steps. Crash safety: step state is DB-persisted; polling recovers. | Yes — revisit if bot process stability becomes a concern |
| D004 | M004 | arch | Shared scheduling primitives location | `packages/orchestration/src/helm_orchestration/scheduling.py` (new file) | Single import point for both `/task` handler and worker job handlers. Keeps timezone, inference, approval policy, and past-event guard co-located. | No |
| D005 | M004 | convention | Operator timezone config | `OPERATOR_TIMEZONE` env var (IANA format), required, fail-fast | Explicit source of truth. No inference from Telegram locale or system TZ. Validated against `zoneinfo` at startup. Visible in `/status` output. | No |
| D006 | M004 | convention | Conditional approval thresholds | Auto-place: confidence ≥ high AND block ≤ 2h AND no displacement. Ask: low confidence, ambiguous sizing, block >2h, conflict/displacement, unclear interpretation | Conservative policy while trust is being rebuilt. Exact confidence threshold is implementation detail for S01. | Yes — loosen once track record is established |
| D007 | M004 | convention | Test layer enforcement | unit: no DB/network; integration: test Postgres, no external API; e2e: real APIs, requires `HELM_E2E=true` + `HELM_CALENDAR_TEST_ID` | Prevents mock leakage that caused timezone bug to go undetected. E2E tests skip (not fail) if env vars absent, but fail explicitly if `HELM_CALENDAR_TEST_ID=primary`. | No |
| D008 | M004 | convention | Telegram default output | Concise operator-facing by default; debug/detail on explicit request | Current verbosity (run IDs, step names, sync timelines) makes Helm feel like a debugging tool. Detail remains accessible via existing `/workflow_sync_detail`, `/workflows` commands. | No |
| D009 | M004 | library | Observability for M004 | `ddtrace` for APM + Datadog log forwarding | Structured JSON logs already present via structlog. `ddtrace` adds APM traces with minimal code changes. Scope bounded to `/task` path + key request handlers. | Yes — expand scope in later milestones |
| D010 | M004 | convention | Live reload mechanism | `watchfiles` for worker and bot | API already uses uvicorn `--reload`. Worker and bot use `python -m watchfiles helm_worker.main src/` pattern. Add `watchfiles` to dev deps. | No |
| D011 | M004/S01 | convention | Pydantic model config for responses.parse targets | `extra="ignore"` (not `"forbid"`) on any model used as `text_format` in `responses.parse` | OpenAI structured output may return extra metadata fields. `extra="forbid"` causes runtime `ValidationError`. `extra="ignore"` silently drops unknown fields. Pattern discovered when adding `TaskSemantics`. | No |
| D012 | M004/S01 | convention | Sync OpenAI SDK in async PTB handlers | `await loop.run_in_executor(None, sync_method, arg)` — never `asyncio.run()` inside async handler | `openai.Client` is synchronous. Calling it directly blocks PTB event loop. `asyncio.run()` inside async context raises `RuntimeError: This event loop is already running`. `run_in_executor` offloads to thread pool without blocking. | No |
| D013 | M004/S01 | convention | Error handling in PTB create_task coroutines | Wrap entire background coroutine body in `try/except Exception` and push user-facing error message in except block | PTB `create_task()` silently discards uncaught exceptions. Without this, operators receive no feedback on failure and errors are invisible. Pattern required for all background tasks created via `create_task()`. | No |
| D014 | M004/S02 | convention | Past-event guard policy | Warn-and-skip (not hard crash): PastEventError moves task to carry_forward, fires structlog WARNING `past_event_guard_triggered` with task_title and ISO UTC timestamp | Hard crash would block the entire schedule proposal on one stale slot. Warn-and-skip surfaces the skipped task in the proposal's carry_forward list while allowing valid slots to proceed. Operator sees the skipped tasks in the approval checkpoint. | Yes — revisit if operators prefer explicit rejection over silently moved tasks |
| D015 | M004/S02 | convention | Time-freeze pattern for scheduling tests | Patch `helm_orchestration.scheduling.datetime`; set `now` to a future Monday at 00:01 UTC (2099-01-05); delegate constructor via `mock_dt.side_effect = lambda *a, **kw: datetime(*a, **kw)` | `past_event_guard` correctly rejects past slots; running tests mid-week (e.g. Tuesday) causes early-week slots to be rejected. Without time-freeze, scheduling tests are flaky (fail Mon–Fri, pass on Monday). Patching the module-level `datetime` is the least-invasive freeze approach. | No — changing import style in scheduling.py would break this pattern |
| D016 | M004/S03 | convention | Cross-package imports from helm_worker in telegram-bot context | Always use lazy imports (inside method body), never module-level imports, when importing from `helm_worker` in the telegram-bot package | `helm_worker.config` instantiates `WorkerSettings()` at module load time, requiring `OPERATOR_TIMEZONE`. A module-level import from `workflow_status_service.py` crashes the bot process when `OPERATOR_TIMEZONE` is absent at import time. Lazy imports defer config instantiation until the method is actually called (which only happens inside the running worker context). | No |
| D017 | M004/S03 | convention | Module-level imports required for monkeypatching | Symbols that tests need to mock via `monkeypatch.setattr` must be module-level imports in the module under test | `monkeypatch.setattr(module, "Symbol", mock)` replaces the module-level name only. Local/lazy imports inside a function capture the original reference and are immune to setattr. Discovered when `LLMClient` in `workflow_runs.py` was moved from a local import inside `_run_task_inference` to module-level to enable test mocking. | No |
| D018 | M005/S01 | schema | user_id FK nullability | Nullable on all domain tables (`ondelete="SET NULL"`) | Existing tests use SQLite in-memory + `Base.metadata.create_all()` with no user seed. NOT NULL would break all 478 existing unit tests. Nullable preserves backward compat; slices S02–S06 populate it explicitly on new records. | Yes — make NOT NULL once all code paths set user_id |
| D019 | M005/S01 | schema | Tables excluded from user_id FK | `job_controls`, `email_agent_configs` (system/config); workflow sub-tables already scoped via parent FK chain | System tables have no per-user semantics. Sub-tables inherit user identity through the parent `workflow_runs.user_id`. Adding FK to sub-tables would be redundant and require chained FK lookups in queries. | No |
| D020 | M005/S01 | schema | Migration strategy for multi-user schema | Schema-wipe (drop_all + create_all) matching prior baseline migrations (0013 pattern) | No existing production data to preserve at this stage. Schema wipe is faster, simpler, and eliminates drift risk vs. incremental ALTER TABLE migrations. | No — once production data exists, incremental ALTERs required |
| D021 | M005/S01 | convention | Bootstrap session ownership | `run_bootstrap()` creates and owns its `SessionLocal`; `bootstrap_user(db)` accepts an injected `Session` | Separating session management from business logic enables unit testing without standing up a real DB. `run_bootstrap()` is the script entry point; `bootstrap_user(db)` is the testable unit. | No |
| D022 | M005/S01 | convention | Bootstrap skip behavior | Skip silently with structlog warning when `TELEGRAM_ALLOWED_USER_ID` is unset; no exception raised | Bootstrap is called on every startup. Raising an exception would block cold starts in environments where credentials aren't yet configured (e.g., fresh clone, CI). Warn-and-continue lets the stack start; missing identity is surfaced in logs. | Yes — revisit if silent skip masks misconfiguration in production |
| D023 | M005/S02 | architecture | google_workspace_mcp credential injection strategy | Bypass `gauth.get_credentials()` entirely — construct `CalendarService`/`GmailService` via `__new__()` and inject `_service` directly using `googleapiclient.discovery.build()` with `google.oauth2.credentials.Credentials` built from `UserCredentialsORM` | `gauth.get_credentials()` reads `GOOGLE_WORKSPACE_*` env vars (different from Helm's `GOOGLE_*`), uses `@lru_cache` (not per-user safe), and is not designed for DB-backed credential injection. Bypassing it by setting `_service` directly on `BaseGoogleService` works because the `service` property returns `_service` directly when non-None, skipping the `gauth` call entirely. This is the cleanest pattern for V1 single-user and scales to multi-user. | No — this is the intended integration pattern for google_workspace_mcp as a library |
| D024 | M005/S02 | library | Token refresh library choice for Google providers | Use `google.oauth2.credentials.Credentials` + `google.auth.transport.requests.Request()` for token refresh; do NOT add `authlib` dependency | `google-auth` is already a transitive dependency of `google-api-python-client` (in dev deps). It provides `Credentials.refresh(Request())` which handles the full OAuth2 token refresh flow. Adding `authlib` would introduce a new dependency (~10+ packages) with no additional value for this use case. The research doc explicitly recommends against `authlib`. | Yes — revisit if multi-provider OAuth (Outlook, etc.) requires authlib's broader protocol support |
| D025 | M005/S03 | convention | submitted_by is stored in raw_request artifact payload, not on WorkflowRunORM | Query `WorkflowArtifactORM` with `artifact_type=raw_request` to get `submitted_by`; never access `WorkflowRunORM.submitted_by` | `WorkflowRunORM` has no `submitted_by` column. The field is in the JSON payload of the `raw_request` artifact row. Any code path that needs the submitter must query `WorkflowArtifactORM` first. This pattern applies to both inline (telegram bot) and recovery (worker) paths. | No |
| D026 | M005/S03 | convention | helm_providers mock patch targets must use importing module namespace | Patch `helm_providers.google_calendar.build` (not `googleapiclient.discovery.build`) and `helm_providers.credentials.Credentials` (not `google.oauth2.credentials.Credentials`) | `patch()` replaces the name in the module namespace where it was imported at module load time. Patching the original source package has no effect once the name is bound. Always patch at the importing module (e.g., `helm_providers.google_calendar.build`) not the originating package. | No |
| D027 | M005/S04 | convention | WorkflowStatusService calendar provider construction strategy | Look up bootstrap user via TELEGRAM_ALLOWED_USER_ID + get_user_by_telegram_id → GoogleCalendarProvider(user.id, session); fall back silently to StubCalendarSystemAdapter when env var is unset | Integration tests that don't set TELEGRAM_ALLOWED_USER_ID must still construct WorkflowStatusService without error. Silent fallback to stub preserves all existing test behaviour while enabling real provider in production. The stub path emits no log warning, which is acceptable for test/dev environments but would need a warning in production. | Yes — add structlog warning on stub fallback path once all tests explicitly set TELEGRAM_ALLOWED_USER_ID |
| D028 | M005/S05 | convention | How to handle cross-package type annotations that would create a circular import at runtime | Use `from __future__ import annotations` + `if TYPE_CHECKING:` guard for annotations that would create a runtime circular import | `helm_providers.gmail` imports `get_credentials` from `helm_storage.repositories.users`, which triggers `helm_storage.repositories.__init__`, which imports `email_messages.py`. If `email_messages.py` imports `NormalizedGmailMessage` from `helm_providers.gmail` at runtime, Python hits a partially initialized module and raises `ImportError`. The `TYPE_CHECKING` guard avoids the cycle: the annotation is only evaluated by static analysis tools (mypy/pyright), never at runtime. Works because all annotated attributes are only accessed as duck-typed plain attributes at runtime — no `isinstance` checks or runtime type introspection. | Yes |
| D029 | M005/S05 | convention | How to unit test worker jobs that construct a provider via the bootstrap-user pattern | Mock all three of `_resolve_bootstrap_user_id`, `SessionLocal`, and `_build_*_provider` together in worker job unit tests | Worker jobs that use the bootstrap-user provider pattern open a `SessionLocal` session, call `_resolve_bootstrap_user_id`, and call `_build_*_provider`. These three are co-required: missing `_resolve_bootstrap_user_id` → `RuntimeError("Bootstrap user not found")`; missing `SessionLocal` → `OperationalError` (real DB attempt in test); missing `_build_*_provider` → real credential lookup in the mocked session. Mocking all three together keeps the DB session for provider construction orthogonal from any real in-memory SQLite session used for runtime assertions. Each missing mock produces a distinct, diagnosable failure, making debugging straightforward. | Yes |
| D030 | M005/S06 | convention | How to add a name to an existing `from X import (...)` block without triggering ruff I001 | Insert the new name alphabetically inside the existing parenthesized import block — never add a second `from X import name` line for the same module | ruff I001 treats two separate `from helm_orchestration import` statements as an unsorted/unformatted import block and reports the entire block as an error. Merging into one alphabetically-sorted block satisfies the linter. For `__init__.py`, submodule order matters too: `schemas` before `stubs` because `sc` < `st`. | No |
| D031 | M005/S06 | convention | How to write test-double builder mocks that won't break when the real function signature grows new keyword args | Use `**_kwargs` in test double signatures for builder functions to future-proof against new keyword arguments | When `workflow_runs.run()` calls `_build_resume_service(session, handlers=handlers, user_id=uid)`, a test mock with signature `(_session, *, handlers)` raises `TypeError: got an unexpected keyword argument 'user_id'`. Using `(_session, *, handlers, **_kwargs)` absorbs future keyword arguments without test breakage, and the `_` prefix signals intentional discard. | No |
