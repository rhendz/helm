---
phase: 03-adapter-writes-and-recovery-guarantees
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/versions/20260313_0009_workflow_sync_records.py
  - packages/storage/src/helm_storage/models.py
  - packages/storage/src/helm_storage/repositories/contracts.py
  - packages/storage/src/helm_storage/repositories/workflow_artifacts.py
  - packages/storage/src/helm_storage/repositories/workflow_events.py
  - packages/storage/src/helm_storage/repositories/__init__.py
  - packages/orchestration/src/helm_orchestration/contracts.py
  - packages/orchestration/src/helm_orchestration/schemas.py
  - packages/orchestration/src/helm_orchestration/workflow_service.py
  - tests/unit/test_workflow_repositories.py
  - tests/unit/test_workflow_orchestration_service.py
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-03
user_setup: []
must_haves:
  truths:
    - "Approved proposal items become durable sync records before any task-system or calendar-system side effect runs."
    - "Task and calendar writes are initiated only through explicit adapter contracts owned by the orchestration boundary, never from workflow logic directly."
    - "Every outbound write candidate is anchored to the exact approved proposal artifact and version so later retries and replays have a stable lineage key."
  artifacts:
    - path: "packages/storage/src/helm_storage/models.py"
      provides: "Workflow sync record ORM model with durable identity, status, idempotency, and lineage fields."
      contains: "workflow_sync_records"
    - path: "packages/storage/src/helm_storage/repositories/contracts.py"
      provides: "Typed sync-record repository contracts and outbound adapter payload types."
    - path: "packages/orchestration/src/helm_orchestration/contracts.py"
      provides: "Task-system and calendar adapter protocols plus normalized sync outcome contracts."
    - path: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      provides: "Kernel logic that derives and persists approved sync work from a concrete proposal version."
    - path: "tests/unit/test_workflow_repositories.py"
      provides: "Repository coverage for sync-record durability, uniqueness, and lineage."
  key_links:
    - from: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      to: "packages/storage/src/helm_storage/repositories/contracts.py"
      via: "Approved proposal execution persists sync records through typed repository operations before any adapter call."
    - from: "packages/orchestration/src/helm_orchestration/contracts.py"
      to: "packages/orchestration/src/helm_orchestration/schemas.py"
      via: "Adapter protocols consume normalized sync item payloads and return typed sync outcomes."
    - from: "migrations/versions/20260313_0009_workflow_sync_records.py"
      to: "packages/storage/src/helm_storage/models.py"
      via: "Migration and ORM align on sync-record identity, status, and lineage columns."
---

<objective>
Establish the durable sync-plan and adapter contract layer for approved workflow writes.

Purpose: Convert approval output into a persisted manifest of outbound task and calendar writes so later execution, retry, resume, and replay paths have stable sync identity and queryable lineage.
Output: Sync-record migration and ORM model, repository contracts, adapter protocols, orchestration support for deriving approved sync items, and tests proving approved proposals become durable write manifests.
</objective>

<execution_context>
@/Users/ankush/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ankush/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-adapter-writes-and-recovery-guarantees/03-CONTEXT.md
@.planning/phases/03-adapter-writes-and-recovery-guarantees/03-RESEARCH.md
@packages/orchestration/src/helm_orchestration/contracts.py
@packages/orchestration/src/helm_orchestration/schemas.py
@packages/orchestration/src/helm_orchestration/workflow_service.py
@packages/storage/src/helm_storage/models.py
@packages/storage/src/helm_storage/repositories/contracts.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add durable sync-record schema and repository contracts</name>
  <files>migrations/versions/20260313_0009_workflow_sync_records.py, packages/storage/src/helm_storage/models.py, packages/storage/src/helm_storage/repositories/contracts.py, packages/storage/src/helm_storage/repositories/__init__.py</files>
  <action>Create a dedicated workflow-native sync table for outbound writes. Include run ID, step ID, approved proposal artifact ID, approved proposal version number, target system, sync kind, planned item key, deterministic execution order, status, idempotency key, external object ID, payload fingerprint, last error summary, last attempted timestamp, completed timestamp, and optional supersedes or replay lineage references. Enforce uniqueness on the stable sync identity and idempotency key at the database level. Add typed repository dataclasses and patch objects for creating sync records, claiming work, persisting outcomes, and querying remaining or failed items. Keep this queryable and boring; do not hide sync identity inside workflow events or final summaries.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_repositories.py -k sync` passes.</verify>
  <done>Storage can durably represent one approved outbound write candidate per task or calendar item with explicit lineage and uniqueness guarantees.</done>
</task>

<task type="auto">
  <name>Task 2: Define adapter contracts and sync-item schemas for approved writes</name>
  <files>packages/orchestration/src/helm_orchestration/contracts.py, packages/orchestration/src/helm_orchestration/schemas.py</files>
  <action>Add explicit task-system and calendar-system adapter protocols plus typed request and response envelopes for outbound writes and reconciliation lookups. Define a stable sync item schema that derives from an approved schedule proposal artifact and carries the exact proposal version anchor, target system, operation kind, deterministic planned item key, and serialized payload fingerprint. The adapter boundary must execute writes only through these contracts, and the orchestration layer must own policy decisions such as ordering, retryability classification, and record mutation. Do not let provider-specific details leak into workflow status projections or storage model names.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py -k adapter` passes.</verify>
  <done>Typed adapter contracts exist for both task and calendar writes, and the approved sync item vocabulary is explicit enough to support later idempotent execution.</done>
</task>

<task type="auto">
  <name>Task 3: Persist approved sync manifests from proposal approval lineage</name>
  <files>packages/storage/src/helm_storage/repositories/workflow_artifacts.py, packages/storage/src/helm_storage/repositories/workflow_events.py, packages/orchestration/src/helm_orchestration/workflow_service.py, tests/unit/test_workflow_repositories.py, tests/unit/test_workflow_orchestration_service.py</files>
  <action>Extend the orchestration service so an approved proposal version resumes into sync preparation by deriving deterministic sync items from the concrete approved artifact and persisting them as sync records before any external call path executes. Record a workflow event that makes the transition from approval resolution to approved sync plan creation inspectable. Ensure task and calendar sync records are stored as separate write units, preserve deterministic ordering, and keep the approved proposal artifact ID as the source-of-truth anchor. Add service and repository tests that prove approval on version N produces stable sync records for version N only and that duplicate preparation attempts do not create duplicate sync rows.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py tests/unit/test_workflow_repositories.py` passes.</verify>
  <done>The kernel turns an approved proposal into a durable sync manifest with task and calendar write units ready for later execution logic.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_repositories.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py`
- [ ] Approved task and calendar writes are represented as sync records before any adapter call path executes.
- [ ] Sync record uniqueness is enforced by durable relational constraints rather than in-memory guards.
- [ ] Adapter contracts exist for both task and calendar writes and are invoked through orchestration-owned abstractions.
- [ ] Sync records are anchored to the concrete approved proposal artifact id and version, not an implied latest proposal.
- [ ] Workflow events or artifacts make sync-manifest creation inspectable without becoming the source of truth for idempotency state.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Approved workflows have a durable, queryable sync manifest that separates policy from provider execution
- Task-system and calendar-system writes can only happen through typed adapter boundaries
- Sync lineage is explicit enough to support duplicate prevention, restart-safe resume, and later replay semantics
  </success_criteria>

<output>
After completion, create `.planning/phases/03-adapter-writes-and-recovery-guarantees/03-01-SUMMARY.md`
</output>
