---
phase: 03-adapter-writes-and-recovery-guarantees
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - migrations/versions/20260313_0011_workflow_recovery_lineage.py
  - packages/storage/src/helm_storage/models.py
  - packages/storage/src/helm_storage/repositories/contracts.py
  - packages/storage/src/helm_storage/repositories/workflow_events.py
  - packages/storage/src/helm_storage/repositories/replay_queue.py
  - packages/orchestration/src/helm_orchestration/schemas.py
  - packages/orchestration/src/helm_orchestration/workflow_service.py
  - tests/unit/test_workflow_orchestration_service.py
  - tests/unit/test_workflow_repositories.py
autonomous: true
requirements:
  - RCVR-03
user_setup: []
must_haves:
  truths:
    - "Retry remains a same-run recovery action, while replay is recorded as an explicit re-execution event with preserved lineage to the prior execution."
    - "Termination after partial sync success stops further outbound writes without deleting or rewriting already-succeeded sync lineage."
    - "Recovery state stays queryable from durable storage and workflow events rather than being inferred from transient worker memory."
  artifacts:
    - path: "migrations/versions/20260313_0011_workflow_recovery_lineage.py"
      provides: "Schema updates for durable recovery and replay lineage fields without overloading retry state."
    - path: "packages/storage/src/helm_storage/repositories/contracts.py"
      provides: "Typed recovery and termination persistence operations for replay lineage, partial termination, and recovery-state queries."
    - path: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      provides: "Recovery classification, replay-event creation, terminate-after-partial-sync handling, and terminal versus recoverable failure semantics."
    - path: "packages/storage/src/helm_storage/repositories/workflow_events.py"
      provides: "Durable workflow events for replay requests, recovery transitions, and termination with partial sync lineage intact."
    - path: "tests/unit/test_workflow_orchestration_service.py"
      provides: "Coverage for replay lineage, same-run retry, and terminate-after-partial-success behavior."
  key_links:
    - from: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      to: "packages/storage/src/helm_storage/repositories/workflow_events.py"
      via: "Recovery, replay, and terminate-after-partial-sync semantics are captured as durable workflow events instead of inferred from status strings."
    - from: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      to: "packages/storage/src/helm_storage/repositories/contracts.py"
      via: "Termination and replay flows mutate recovery state through typed repository operations rather than ad hoc step-state edits."
---

<objective>
Add explicit replay lineage and terminate-safe recovery semantics inside the durable workflow kernel.

Purpose: Make recovery semantics explicit in storage and orchestration so retry, replay, termination, and partial sync success remain durable and unambiguous before any operator surface projects them.
Output: Recovery and replay lineage model updates, workflow-service semantics for same-run retry and terminate-after-partial-success, and tests proving history stays intact across those transitions.
</objective>

<execution_context>
@/Users/ankush/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ankush/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-adapter-writes-and-recovery-guarantees/03-CONTEXT.md
@.planning/phases/03-adapter-writes-and-recovery-guarantees/03-RESEARCH.md
@.planning/phases/03-adapter-writes-and-recovery-guarantees/03-02-PLAN.md
@packages/orchestration/src/helm_orchestration/workflow_service.py
@packages/storage/src/helm_storage/repositories/contracts.py
@packages/storage/src/helm_storage/repositories/workflow_events.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Persist recovery classification and replay lineage separately from retry</name>
  <files>migrations/versions/20260313_0011_workflow_recovery_lineage.py, packages/storage/src/helm_storage/models.py, packages/storage/src/helm_storage/repositories/contracts.py, packages/storage/src/helm_storage/repositories/workflow_events.py, packages/storage/src/helm_storage/repositories/replay_queue.py, packages/orchestration/src/helm_orchestration/schemas.py, packages/orchestration/src/helm_orchestration/workflow_service.py</files>
  <action>Add durable fields, events, or typed payloads that make recoverable sync failure, terminal sync failure, retry, replay request, and replay lineage explicit states instead of implicit interpretations of generic failure text. Keep retry as a same-run step re-attempt that preserves prior sync records. Add replay lineage that links the deliberate re-execution request to the original run or step and, when needed, to the prior sync records it is superseding or rechecking. Ensure the workflow service records replay as a separate operator-intended event and does not let ordinary failed-step retry mutate history into an apparent replay.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py -k 'replay or recoverable or terminal'` passes.</verify>
  <done>Recovery semantics are durable and explicit enough that storage and orchestration can distinguish retry from replay and recoverable from terminal failure.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve partial-sync lineage when termination halts remaining writes</name>
  <files>packages/storage/src/helm_storage/repositories/contracts.py, packages/storage/src/helm_storage/repositories/workflow_events.py, packages/orchestration/src/helm_orchestration/workflow_service.py, tests/unit/test_workflow_orchestration_service.py, tests/unit/test_workflow_repositories.py</files>
  <action>Implement termination semantics for sync execution so a terminate or approval-revocation action after partial success halts any remaining outbound writes, leaves already-succeeded sync rows untouched, and records a terminal workflow transition that preserves partial sync counts and last attempted item references for later projection. Ensure termination never rewrites prior success into failure and never schedules additional writes after the terminate transition has been persisted.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py -k 'terminate or partial'` passes.</verify>
  <done>Terminate-after-partial-success behavior is explicit in the kernel and preserves durable sync lineage instead of collapsing into a generic terminated state.</done>
</task>

<task type="auto">
  <name>Task 3: Keep replay and same-run retry semantics distinct in tests and durable events</name>
  <files>packages/storage/src/helm_storage/repositories/workflow_events.py, packages/orchestration/src/helm_orchestration/workflow_service.py, tests/unit/test_workflow_orchestration_service.py</files>
  <action>Add focused orchestration coverage that proves failed-step retry within the same run reuses existing sync lineage, while explicit replay appends new lineage and recovery events without mutating prior rows in place. Include scenarios where a replay follows partial sync success and where a terminate action has already frozen remaining writes, so the kernel behavior is settled before app-layer entrypoints are added.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py -k 'retry or replay or terminate'` passes.</verify>
  <done>The durable kernel behavior for retry, replay, and termination is locked down before API and Telegram operator surfaces are wired on top.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_repositories.py`
- [ ] Recoverable and terminal sync failures are distinguishable from durable state before any app-layer projection consumes them.
- [ ] Replay is recorded as an explicit lineage event and is not interchangeable with failed-step retry.
- [ ] Termination after partial success preserves succeeded sync rows, stops remaining writes, and records a terminal transition with durable partial lineage.
- [ ] Same-run retry and explicit replay remain distinct in storage and orchestration even when they target the same approved proposal version.
- [ ] Recovery and replay facts are stored in durable workflow records or events so later status projections do not need to infer semantics from free-form error text.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Replay semantics are explicit, lineage-preserving, and clearly distinct from retry semantics inside the workflow kernel
- Termination after partial sync success is durable, inspectable, and does not corrupt prior sync lineage
- App-layer projections can be added on top of explicit recovery facts rather than reconstructing kernel behavior from ambiguous state
  </success_criteria>

<output>
After completion, create `.planning/phases/03-adapter-writes-and-recovery-guarantees/03-03-SUMMARY.md`
</output>
