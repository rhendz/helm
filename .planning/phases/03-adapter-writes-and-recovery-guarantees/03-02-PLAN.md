---
phase: 03-adapter-writes-and-recovery-guarantees
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - migrations/versions/20260313_0010_workflow_sync_execution.py
  - packages/storage/src/helm_storage/models.py
  - packages/storage/src/helm_storage/repositories/contracts.py
  - packages/storage/src/helm_storage/repositories/workflow_runs.py
  - packages/storage/src/helm_storage/repositories/workflow_steps.py
  - packages/storage/src/helm_storage/repositories/workflow_events.py
  - packages/storage/src/helm_storage/repositories/__init__.py
  - packages/connectors/src/helm_connectors/__init__.py
  - packages/connectors/src/helm_connectors/task_system.py
  - packages/connectors/src/helm_connectors/calendar_system.py
  - packages/orchestration/src/helm_orchestration/schemas.py
  - packages/orchestration/src/helm_orchestration/workflow_service.py
  - packages/orchestration/src/helm_orchestration/resume_service.py
  - apps/worker/src/helm_worker/jobs/workflow_runs.py
  - tests/unit/test_workflow_orchestration_service.py
  - tests/unit/test_workflow_repositories.py
autonomous: true
requirements:
  - SYNC-04
  - SYNC-05
  - SYNC-06
  - RCVR-01
  - RCVR-02
user_setup: []
must_haves:
  truths:
    - "Retry and resume paths rebuild remaining adapter work from durable sync facts instead of in-memory progress."
    - "Duplicate downstream writes are prevented across failure retry and post-restart resume by stable sync identity and idempotency data."
    - "Uncertain write outcomes reconcile against persisted identity before Helm attempts another create, update, or delete."
  artifacts:
    - path: "packages/connectors/src/helm_connectors/task_system.py"
      provides: "Package-layer stub task-system adapter implementation that satisfies orchestration contracts without moving recovery policy out of orchestration."
    - path: "packages/connectors/src/helm_connectors/calendar_system.py"
      provides: "Package-layer stub calendar-system adapter implementation that gives sync execution a concrete boundary for deterministic dispatch tests."
    - path: "packages/connectors/src/helm_connectors/__init__.py"
      provides: "Connector composition entrypoint that exports the task and calendar adapter implementations for worker and orchestration wiring."
    - path: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      provides: "Execution path for approved sync records with deterministic ordering, claim-and-update semantics, and outcome classification."
    - path: "packages/orchestration/src/helm_orchestration/resume_service.py"
      provides: "Restart-safe resume dispatch for adapter-write steps that derives next work from persisted sync state."
    - path: "packages/storage/src/helm_storage/repositories/contracts.py"
      provides: "Repository methods for claim, reconcile-needed lookup, outcome persistence, and failed-item retry selection."
    - path: "apps/worker/src/helm_worker/jobs/workflow_runs.py"
      provides: "Worker wiring that resumes runnable sync steps without duplicating orchestration logic."
    - path: "tests/unit/test_workflow_orchestration_service.py"
      provides: "Coverage for duplicate-prevention, reconciliation-first recovery, and step retry semantics."
  key_links:
    - from: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      to: "packages/connectors/src/helm_connectors/task_system.py"
      via: "Orchestration dispatches task sync items through a package-layer adapter implementation instead of embedding provider logic in the app or workflow layer."
    - from: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      to: "packages/connectors/src/helm_connectors/calendar_system.py"
      via: "Calendar sync execution resolves through a package-layer adapter boundary whose implementation remains separate from retry and resume policy."
    - from: "packages/orchestration/src/helm_orchestration/resume_service.py"
      to: "packages/storage/src/helm_storage/repositories/contracts.py"
      via: "Resume queries remaining sync records and unresolved outcomes from durable storage."
    - from: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      to: "apps/worker/src/helm_worker/jobs/workflow_runs.py"
      via: "Worker-triggered resume re-enters orchestration-owned sync execution instead of ad hoc job logic."
    - from: "packages/storage/src/helm_storage/repositories/workflow_runs.py"
      to: "packages/storage/src/helm_storage/repositories/workflow_steps.py"
      via: "Failed-step retry stays within the same run while preserving prior step attempts and sync records."
---

<objective>
Implement idempotent adapter execution, reconciliation-first recovery, and restart-safe resume for approved sync work.

Purpose: Make the Phase 03 sync manifest executable without duplicate side effects by ensuring the worker and orchestration layers always recover from durable sync records and only retry unresolved or failed items.
Output: Sync execution logic, durable outcome states, resume-service integration, worker wiring, and tests covering retry, restart, and duplicate-prevention behavior.
</objective>

<execution_context>
@/Users/ankush/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ankush/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-adapter-writes-and-recovery-guarantees/03-CONTEXT.md
@.planning/phases/03-adapter-writes-and-recovery-guarantees/03-RESEARCH.md
@.planning/phases/03-adapter-writes-and-recovery-guarantees/03-01-PLAN.md
@packages/connectors/src/helm_connectors/__init__.py
@packages/orchestration/src/helm_orchestration/workflow_service.py
@packages/orchestration/src/helm_orchestration/resume_service.py
@packages/storage/src/helm_storage/repositories/contracts.py
@apps/worker/src/helm_worker/jobs/workflow_runs.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add durable sync execution states and claim-or-update repository flows</name>
  <files>migrations/versions/20260313_0010_workflow_sync_execution.py, packages/storage/src/helm_storage/models.py, packages/storage/src/helm_storage/repositories/contracts.py, packages/storage/src/helm_storage/repositories/workflow_runs.py, packages/storage/src/helm_storage/repositories/workflow_steps.py, packages/storage/src/helm_storage/repositories/workflow_events.py, packages/storage/src/helm_storage/repositories/__init__.py</files>
  <action>Extend sync records and repository contracts so execution can safely transition through `pending`, `in_progress`, `succeeded`, `failed_retryable`, `failed_terminal`, and `uncertain_needs_reconciliation` states. Add transactional repository methods that claim the next eligible sync item, persist an adapter attempt start, classify completion or failure, and look up unresolved work for a specific run and step attempt. When a step is retried, prior sync state must remain intact and the next attempt must select only unresolved or retryable items. Avoid any repository API that depends on a caller-provided in-memory cursor such as "process item number N".</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_repositories.py -k 'sync and retry'` passes.</verify>
  <done>Storage exposes enough durable execution state to drive idempotent sync processing and re-attempt selection without transient progress tracking.</done>
</task>

<task type="auto">
  <name>Task 2: Execute approved sync records through adapters with reconciliation before retry</name>
  <files>packages/connectors/src/helm_connectors/__init__.py, packages/connectors/src/helm_connectors/task_system.py, packages/connectors/src/helm_connectors/calendar_system.py, packages/orchestration/src/helm_orchestration/schemas.py, packages/orchestration/src/helm_orchestration/workflow_service.py, tests/unit/test_workflow_orchestration_service.py</files>
  <action>Implement orchestration logic that iterates persisted sync records in deterministic order, dispatches each item through the correct adapter, and persists outcomes immediately after each call. The concrete task-system and calendar-system execution for this plan lives in the package layer as stub adapter implementations under `packages/connectors`, exported through the package `__init__` composition entrypoint, while orchestration keeps ownership of ordering, retryability, and record mutation. If an earlier attempt left a record in an uncertain state, call the adapter reconciliation contract first and mark the item succeeded if the external object already exists or matches the stored fingerprint. Only attempt a new create, update, or delete when reconciliation shows the prior write did not complete. Ensure retryable failures stop the step with partial sync lineage preserved, and ensure already-succeeded items are skipped on later retries and resumes. Keep task-first versus calendar-first ordering deterministic and explicit in code and tests.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py -k 'sync or reconciliation or retry'` passes.</verify>
  <done>Approved sync work executes idempotently through explicit package-layer task and calendar adapter stubs, records partial progress durably, and reconciles uncertain outcomes before another outbound write occurs.</done>
</task>

<task type="auto">
  <name>Task 3: Make resume and worker execution rebuild state from persisted sync facts</name>
  <files>packages/orchestration/src/helm_orchestration/resume_service.py, apps/worker/src/helm_worker/jobs/workflow_runs.py, tests/unit/test_workflow_orchestration_service.py, tests/unit/test_workflow_repositories.py</files>
  <action>Teach the resume service and worker workflow job to recognize sync execution steps and resume them by loading durable sync records for the current run rather than assuming no side effects occurred. Add tests that simulate process restart after some sync items succeed, after a write result becomes uncertain, and after a retryable failure stops the run. Verify that restart-safe resume and explicit step retry both preserve lineage, skip previously succeeded items, reconcile uncertain items, and re-attempt only remaining work within the same workflow run. Keep resume semantics in the orchestration package; the worker should only ask the resume service to process runnable runs.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py tests/unit/test_workflow_repositories.py` passes.</verify>
  <done>Worker restart and operator retry both rebuild sync execution from persisted state without creating duplicate downstream objects or losing prior lineage.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_repositories.py`
- [ ] Adapter execution skips already-succeeded sync records on both retry and post-restart resume.
- [ ] Uncertain outcomes trigger reconciliation before another write attempt is made.
- [ ] Retryable failures preserve partial sync lineage and stop the step with enough data to continue safely.
- [ ] Resume logic determines remaining work entirely from persisted sync records and current step state.
- [ ] Duplicate prevention is enforced across both failed-step retry and paused or interrupted workflow resume.
- [ ] Concrete task and calendar adapter execution remains in `packages/connectors`, with app layers only wiring orchestration entrypoints.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Helm can resume and retry approved sync execution without losing lineage or duplicating downstream objects
- Idempotency keys and durable sync identity, not prompt memory or in-process counters, are the foundation of recovery
- Partial success remains inspectable and safe for later recovery actions
  </success_criteria>

<output>
After completion, create `.planning/phases/03-adapter-writes-and-recovery-guarantees/03-02-SUMMARY.md`
</output>
