---
phase: 03-adapter-writes-and-recovery-guarantees
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - apps/api/src/helm_api/services/workflow_status_service.py
  - tests/unit/test_workflow_status_service.py
autonomous: true
requirements:
  - OPER-01
user_setup: []
must_haves:
  truths:
    - "Operator surfaces present a compact pre-execution effect summary before adapter execution begins, including how many task and calendar writes approval will trigger."
    - "API and Telegram surfaces consume one shared workflow status projection for sync counts, partial success, recovery class, and safe next actions."
    - "The shared projection reads durable sync and recovery facts directly, so app entrypoints stay thin and consistent."
  artifacts:
    - path: "apps/api/src/helm_api/services/workflow_status_service.py"
      provides: "Shared read-model projection for pre-execution effect summaries, sync counts by state, last failed item, recovery class, replay lineage, and next safe actions."
    - path: "tests/unit/test_workflow_status_service.py"
      provides: "Coverage for effect-summary, partial-sync, recoverable-failure, terminal-failure, and replay-lineage projection contracts."
  key_links:
    - from: "apps/api/src/helm_api/services/workflow_status_service.py"
      to: "tests/unit/test_workflow_status_service.py"
      via: "Projection tests lock the effect summary, partial-sync, and replay-lineage shapes before multiple app entrypoints depend on them."
---

<objective>
Expose Phase 03 recovery semantics through one shared workflow status projection.

Purpose: Make the durable sync and recovery facts from earlier Phase 03 plans legible to operators before execution and after partial success, while keeping the projection itself separate from API, worker, and Telegram entrypoint wiring.
Output: Shared workflow status projection updates and tests proving operators can inspect effect summaries, partial sync, and recovery state from one durable read model.
</objective>

<execution_context>
@/Users/ankush/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ankush/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-adapter-writes-and-recovery-guarantees/03-CONTEXT.md
@.planning/phases/03-adapter-writes-and-recovery-guarantees/03-RESEARCH.md
@.planning/phases/03-adapter-writes-and-recovery-guarantees/03-03-PLAN.md
@apps/api/src/helm_api/services/workflow_status_service.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extend the shared workflow status projection with effect and recovery summaries</name>
  <files>apps/api/src/helm_api/services/workflow_status_service.py, tests/unit/test_workflow_status_service.py</files>
  <action>Update the shared workflow status projection so it exposes a compact pre-execution effect summary after approval and sync-manifest creation, including the number of task and calendar writes Helm will attempt before execution begins. Extend the same projection with sync counts by state, the last failed or unresolved sync item, failure class, retryability, replay lineage when present, and the next safe operator actions. Keep the projection thin by reading durable sync and recovery facts rather than embedding provider payload dumps or duplicating orchestration business rules.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_status_service.py -k 'effect or sync or replay'` passes.</verify>
  <done>One shared read model can explain both pending approved effects and post-failure recovery state to API and Telegram consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Project partial-sync and termination outcomes without raw-event inspection</name>
  <files>apps/api/src/helm_api/services/workflow_status_service.py, tests/unit/test_workflow_status_service.py</files>
  <action>Add projection logic that keeps partial sync visible after termination, retry, or replay requests by summarizing durable sync counts, last failed item, and recovery class without forcing callers to inspect raw workflow events. Ensure the projection distinguishes recoverable failure from terminal failure and carries replay lineage as summary metadata rather than treating event text as the source of truth.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_status_service.py -k 'partial or terminal or lineage'` passes.</verify>
  <done>Operators can understand partial sync and terminated states from the shared projection alone, with no app-specific recovery logic.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the status projection contract before app-layer entrypoints consume it</name>
  <files>apps/api/src/helm_api/services/workflow_status_service.py, tests/unit/test_workflow_status_service.py</files>
  <action>Refine the status projection and its tests so the pre-execution effect summary, sync-state counts, safe-next-action hints, and replay-lineage fields form a stable contract for later API and Telegram wiring. Keep this plan scoped to the shared read-model boundary only; do not register replay routes or bot commands here.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_status_service.py` passes.</verify>
  <done>The shared workflow status contract is explicit and tested before any app entrypoint is wired to it.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_status_service.py`
- [ ] The shared workflow status projection exposes pending approved task and calendar write counts before execution begins.
- [ ] Partial-sync outcomes remain visible after termination, retry, or replay requests.
- [ ] Replay lineage, recovery class, and safe-next-action hints are projected from durable facts instead of inferred from free-form event text.
- [ ] The projection contract is explicit enough that later API and Telegram entrypoints can consume it without re-implementing business rules.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Operators can tell what approval will do, what already synced, what failed, whether the failure is recoverable, and what action is safe next from one shared read model
- API and Telegram surfaces can stay thin because they will consume an explicit, tested workflow status projection
- The compact effect summary requirement is satisfied before app-layer wiring is added
  </success_criteria>

<output>
After completion, create `.planning/phases/03-adapter-writes-and-recovery-guarantees/03-04-SUMMARY.md`
</output>
