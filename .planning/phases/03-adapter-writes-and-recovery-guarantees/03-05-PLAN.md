---
phase: 03-adapter-writes-and-recovery-guarantees
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - apps/api/src/helm_api/main.py
  - apps/api/src/helm_api/routers/replay.py
  - apps/api/src/helm_api/schemas.py
  - apps/api/src/helm_api/services/replay_service.py
  - apps/telegram-bot/src/helm_telegram_bot/main.py
  - apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py
  - apps/worker/src/helm_worker/jobs/replay.py
  - tests/unit/test_replay_service.py
  - tests/unit/test_workflow_status_service.py
autonomous: true
requirements:
  - RCVR-04
  - OPER-01
user_setup: []
must_haves:
  truths:
    - "Replay remains a deliberate operator action exposed through explicit API and Telegram entrypoints, never a generic retry button."
    - "API, worker, and Telegram entrypoints stay thin by consuming the shared workflow status projection and kernel replay semantics from earlier plans."
    - "Operator-triggered replay must be registered end-to-end so it is actually reachable from the shipped app entrypoints."
  artifacts:
    - path: "apps/api/src/helm_api/services/replay_service.py"
      provides: "Workflow replay entry point that translates operator intent into lineage-preserving replay work."
    - path: "apps/api/src/helm_api/routers/replay.py"
      provides: "Typed FastAPI replay endpoints wired to the replay service."
    - path: "apps/api/src/helm_api/main.py"
      provides: "Application registration for replay routes so the API surface is reachable end-to-end."
    - path: "apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py"
      provides: "Telegram-friendly workflow recovery actions built from the shared status projection."
    - path: "apps/telegram-bot/src/helm_telegram_bot/main.py"
      provides: "Bot command registration for workflow replay and recovery actions."
    - path: "apps/worker/src/helm_worker/jobs/replay.py"
      provides: "Worker replay job wiring that consumes replay requests without duplicating API or Telegram decision logic."
    - path: "tests/unit/test_replay_service.py"
      provides: "Coverage for API-triggered replay requests and worker handoff behavior."
  key_links:
    - from: "apps/api/src/helm_api/main.py"
      to: "apps/api/src/helm_api/routers/replay.py"
      via: "Replay endpoints are registered in the API app so operator-triggered replay requests are actually reachable."
    - from: "apps/api/src/helm_api/routers/replay.py"
      to: "apps/api/src/helm_api/schemas.py"
      via: "Replay routes accept and return typed workflow replay payloads rather than ad hoc request bodies."
    - from: "apps/api/src/helm_api/services/replay_service.py"
      to: "apps/worker/src/helm_worker/jobs/replay.py"
      via: "Replay requests enqueue deliberate re-execution work with explicit lineage to prior sync execution."
    - from: "apps/telegram-bot/src/helm_telegram_bot/main.py"
      to: "apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py"
      via: "Telegram recovery and replay commands are registered in the bot application to be operator-triggerable."
---

<objective>
Wire operator-safe replay and recovery entrypoints on top of the shared Phase 03 status and kernel semantics.

Purpose: Make deliberate replay and recovery controls reachable through API, worker, and Telegram entrypoints without duplicating the durable recovery rules established earlier in the phase.
Output: API replay endpoints, replay service wiring, worker replay job integration, Telegram recovery commands, and tests proving operator-triggered replay is reachable end-to-end.
</objective>

<execution_context>
@/Users/ankush/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ankush/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-adapter-writes-and-recovery-guarantees/03-CONTEXT.md
@.planning/phases/03-adapter-writes-and-recovery-guarantees/03-RESEARCH.md
@.planning/phases/03-adapter-writes-and-recovery-guarantees/03-04-PLAN.md
@apps/api/src/helm_api/main.py
@apps/api/src/helm_api/routers/replay.py
@apps/api/src/helm_api/schemas.py
@apps/api/src/helm_api/services/replay_service.py
@apps/telegram-bot/src/helm_telegram_bot/main.py
@apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py
@apps/worker/src/helm_worker/jobs/replay.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add registered API replay entrypoints on top of the kernel replay model</name>
  <files>apps/api/src/helm_api/main.py, apps/api/src/helm_api/routers/replay.py, apps/api/src/helm_api/schemas.py, apps/api/src/helm_api/services/replay_service.py, tests/unit/test_replay_service.py</files>
  <action>Wire explicit replay handling for workflow sync execution through the existing replay service, but constrain it so replay remains an intentional operator action with preserved lineage and reconciliation against known sync facts. Expand the API surface all the way through the concrete operator entrypoints by updating the replay router, its typed request or response schemas, and API app registration so API-triggered replay requests are actually reachable end-to-end. Keep the API layer thin by translating operator intent into the durable kernel semantics from Plans 03 and 04 instead of re-implementing replay policy in the router.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_replay_service.py -k api` passes.</verify>
  <done>API-triggered replay is reachable end-to-end and remains a lineage-preserving operator action instead of an overloaded retry shortcut.</done>
</task>

<task type="auto">
  <name>Task 2: Wire worker replay execution without duplicating app-layer policy</name>
  <files>apps/api/src/helm_api/services/replay_service.py, apps/worker/src/helm_worker/jobs/replay.py, tests/unit/test_replay_service.py</files>
  <action>Connect replay requests to the worker replay job path so the worker consumes explicit replay work and existing kernel semantics rather than making its own recovery decisions. Keep the worker job responsible only for replay execution handoff and observability, not for inventing new retry or replay policy.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_replay_service.py -k worker` passes.</verify>
  <done>Worker replay execution is wired end-to-end without creating a second source of replay policy.</done>
</task>

<task type="auto">
  <name>Task 3: Surface replay and recovery actions in Telegram using the shared status projection</name>
  <files>apps/telegram-bot/src/helm_telegram_bot/main.py, apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py, tests/unit/test_workflow_status_service.py</files>
  <action>Update Telegram workflow commands to show recovery actions based on the shared workflow status projection, present replay as a distinct action from retry, and register those commands in the bot `main.py` entrypoint so they are actually triggerable from Telegram. Keep Telegram behavior aligned with the API surface by consuming the shared workflow status contract from Plan 04 instead of duplicating sync, retry, or replay decision rules in the bot layer.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_status_service.py -k telegram` passes.</verify>
  <done>Telegram operators can trigger the correct replay or recovery action without divergent bot-only logic.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_replay_service.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_status_service.py`
- [ ] API replay routes and Telegram replay commands are registered in their application entrypoints, not only implemented in service or command modules.
- [ ] Operator-triggered replay remains clearly distinct from retry across API and Telegram surfaces.
- [ ] API, worker, and Telegram entrypoints consume existing kernel and status semantics instead of re-implementing recovery rules.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Replay is reachable end-to-end from the actual operator entrypoints
- API, worker, and Telegram surfaces stay thin because they build on shared Phase 03 semantics
- Operators can trigger deliberate replay without blurring it into retry
  </success_criteria>

<output>
After completion, create `.planning/phases/03-adapter-writes-and-recovery-guarantees/03-05-SUMMARY.md`
</output>
