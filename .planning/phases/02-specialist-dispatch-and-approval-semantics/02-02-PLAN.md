---
phase: 02-specialist-dispatch-and-approval-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - migrations/versions/20260313_0009_approval_checkpoints.py
  - packages/storage/src/helm_storage/models.py
  - packages/storage/src/helm_storage/repositories/contracts.py
  - packages/storage/src/helm_storage/repositories/workflow_artifacts.py
  - packages/storage/src/helm_storage/repositories/workflow_runs.py
  - packages/storage/src/helm_storage/repositories/workflow_events.py
  - packages/orchestration/src/helm_orchestration/schemas.py
  - packages/orchestration/src/helm_orchestration/workflow_service.py
  - packages/orchestration/src/helm_orchestration/resume_service.py
  - apps/api/src/helm_api/schemas.py
  - apps/api/src/helm_api/routers/workflow_runs.py
  - apps/api/src/helm_api/services/workflow_status_service.py
  - apps/telegram-bot/src/helm_telegram_bot/services/workflow_status_service.py
  - apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py
  - apps/telegram-bot/src/helm_telegram_bot/commands/approve.py
  - tests/unit/test_workflow_orchestration_service.py
  - tests/unit/test_workflow_status_service.py
  - tests/unit/test_telegram_commands.py
  - tests/integration/test_workflow_status_routes.py
autonomous: true
requirements:
  - APRV-01
  - APRV-02
  - APRV-03
  - APRV-04
user_setup: []
must_haves:
  truths:
    - "Helm pauses on a valid schedule proposal before any downstream create, update, or delete action."
    - "Approval checkpoints are durable workflow state, not ephemeral Telegram messages or read-model placeholders."
    - "An operator can approve, reject, or request revision through shared kernel semantics, and the workflow resumes from the correct persisted step boundary."
    - "API and Telegram surfaces expose the same checkpoint state, allowed actions, and last decision outcome from one read model."
  artifacts:
    - path: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      provides: "Approval checkpoint creation, durable decision handling, and pause/resume state transitions."
    - path: "packages/orchestration/src/helm_orchestration/schemas.py"
      provides: "Typed approval request and approval decision payloads including allowed actions and operator feedback."
    - path: "apps/api/src/helm_api/services/workflow_status_service.py"
      provides: "Checkpoint-aware read model for API and Telegram projection."
    - path: "apps/telegram-bot/src/helm_telegram_bot/commands/approve.py"
      provides: "Telegram approval and rejection/revision command entrypoints aligned to kernel actions."
    - path: "tests/integration/test_workflow_status_routes.py"
      provides: "HTTP verification for approval decision routes and post-decision resume behavior."
  key_links:
    - from: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      to: "packages/storage/src/helm_storage/repositories/workflow_runs.py"
      via: "Approval checkpoints update blocked state, needs-action flags, and resume-safe step pointers atomically."
    - from: "apps/api/src/helm_api/routers/workflow_runs.py"
      to: "apps/api/src/helm_api/services/workflow_status_service.py"
      via: "Approval routes and status inspection share one checkpoint read model."
    - from: "apps/telegram-bot/src/helm_telegram_bot/commands/approve.py"
      to: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      via: "Telegram decisions call kernel-owned approval semantics rather than mutating records directly."
---

<objective>
Implement first-class approval checkpoints, operator decisions, and approval-driven resume semantics for schedule proposals.

Purpose: Phase 2 requires valid proposal artifacts to pause for human decision before any later side-effect phase can act on them. This plan turns approval into a durable kernel state with shared API and Telegram operator paths.
Output: Approval storage/contracts, checkpoint creation and decision handling, shared read-model updates, API routes, Telegram commands, and tests for approve/reject/revision resume behavior.
</objective>

<execution_context>
@/Users/ankush/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ankush/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-specialist-dispatch-and-approval-semantics/02-CONTEXT.md
@.planning/phases/02-specialist-dispatch-and-approval-semantics/02-RESEARCH.md
@.planning/phases/02-specialist-dispatch-and-approval-semantics/02-01-PLAN.md
@apps/api/src/helm_api/services/workflow_status_service.py
@apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py
@apps/telegram-bot/src/helm_telegram_bot/commands/approve.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add durable approval checkpoint and decision persistence</name>
  <files>migrations/versions/20260313_0009_approval_checkpoints.py, packages/storage/src/helm_storage/models.py, packages/storage/src/helm_storage/repositories/contracts.py, packages/storage/src/helm_storage/repositories/workflow_artifacts.py, packages/storage/src/helm_storage/repositories/workflow_runs.py, packages/storage/src/helm_storage/repositories/workflow_events.py</files>
  <action>Add the durable storage shape for approval requests and decisions. Persist the target proposal artifact version, allowed actions, current checkpoint status, final decision, actor, decision timestamp, and revision feedback when present. The persistence model may use workflow artifacts, a dedicated approval table, or both, but the durable contract must be queryable enough to answer which proposal version is awaiting action and which decision resolved it. Update run and event persistence so approval checkpoints move the run into a blocked needs-action state that is distinct from validation failure and ordinary execution failure.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_repositories.py -k approval` passes.</verify>
  <done>Storage can represent pending approval checkpoints and resolved decisions with explicit linkage to the proposal version under review.</done>
</task>

<task type="auto">
  <name>Task 2: Implement checkpoint creation, operator decision handling, and resume semantics in the kernel</name>
  <files>packages/orchestration/src/helm_orchestration/schemas.py, packages/orchestration/src/helm_orchestration/workflow_service.py, packages/orchestration/src/helm_orchestration/resume_service.py, tests/unit/test_workflow_orchestration_service.py</files>
  <action>Extend the workflow kernel so a successful schedule proposal step creates an approval checkpoint artifact or record, marks the run as blocked with an explicit approval pause reason, and excludes it from runnable worker scans. Add kernel operations for approve, reject, and request revision. Approve must resolve the checkpoint and advance the run to the next persisted step. Reject must resolve the checkpoint and terminate or close the run cleanly without re-entering proposal generation. Request revision must resolve the active checkpoint, persist the revision feedback, and set the run up to resume from the proposal-producing specialist step rather than from the workflow beginning. Do not require an extra manual resume action after decision submission.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py -k approval` passes.</verify>
  <done>The kernel owns approval pause/resume semantics and resumes from the correct step for every allowed decision path.</done>
</task>

<task type="auto">
  <name>Task 3: Expose approval checkpoints through shared API and Telegram operator paths</name>
  <files>apps/api/src/helm_api/schemas.py, apps/api/src/helm_api/routers/workflow_runs.py, apps/api/src/helm_api/services/workflow_status_service.py, apps/telegram-bot/src/helm_telegram_bot/services/workflow_status_service.py, apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py, apps/telegram-bot/src/helm_telegram_bot/commands/approve.py, tests/unit/test_workflow_status_service.py, tests/unit/test_telegram_commands.py, tests/integration/test_workflow_status_routes.py</files>
  <action>Project pending approval checkpoints into the shared workflow status read model, including proposal summary, pause reason, allowed actions, checkpoint ID, and latest decision outcome when present. Add API decision routes and Telegram commands for approve, reject, and request revision using the existing thin-surface pattern. Keep Telegram rendering concise and action-oriented: what Helm proposes, why it paused, and what each action does next. Add route and command tests covering pending approval, approve-to-resume, reject-to-close, and revision-request submission without duplicating business rules across app layers.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_status_service.py tests/unit/test_telegram_commands.py tests/integration/test_workflow_status_routes.py` passes.</verify>
  <done>Operators can inspect and resolve approval checkpoints consistently from API or Telegram, and the run state changes are sourced from the same kernel semantics.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_status_service.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_telegram_commands.py`
- [ ] `uv run --frozen --extra dev pytest tests/integration/test_workflow_status_routes.py`
- [ ] A valid schedule proposal creates a durable approval checkpoint and moves the run into an approval-blocked state before any downstream write step
- [ ] Operators can approve, reject, or request revision through API and Telegram paths backed by one kernel decision model
- [ ] Approval requests persist allowed actions, final decision, actor, and decision timestamp
- [ ] Decision submission resumes the run automatically from the correct persisted step boundary without a second operator resume command
- [ ] Approval-blocked state remains distinct from validation-blocked and execution-failed run states in read models and events
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Approval semantics are durable, inspectable, and reusable across operator surfaces
- Human decision is required before any meaningful outbound write path can proceed in later phases
- Approval and resume behavior is implemented once in the kernel and projected outward, not reimplemented per app
  </success_criteria>

<output>
After completion, create `.planning/phases/02-specialist-dispatch-and-approval-semantics/02-02-SUMMARY.md`
</output>
