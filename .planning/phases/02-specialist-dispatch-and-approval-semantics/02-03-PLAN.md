---
phase: 02-specialist-dispatch-and-approval-semantics
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - packages/storage/src/helm_storage/repositories/contracts.py
  - packages/storage/src/helm_storage/repositories/workflow_artifacts.py
  - packages/storage/src/helm_storage/repositories/workflow_events.py
  - packages/orchestration/src/helm_orchestration/schemas.py
  - packages/orchestration/src/helm_orchestration/workflow_service.py
  - packages/orchestration/src/helm_orchestration/resume_service.py
  - apps/api/src/helm_api/schemas.py
  - apps/api/src/helm_api/services/workflow_status_service.py
  - apps/api/src/helm_api/routers/workflow_runs.py
  - apps/telegram-bot/src/helm_telegram_bot/services/workflow_status_service.py
  - apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py
  - apps/telegram-bot/src/helm_telegram_bot/commands/approve.py
  - docs/runbooks/workflow-runs.md
  - tests/unit/test_workflow_orchestration_service.py
  - tests/unit/test_workflow_status_service.py
  - tests/unit/test_telegram_commands.py
  - tests/integration/test_workflow_status_routes.py
autonomous: true
requirements:
  - APRV-05
  - APRV-06
  - ARTF-04
user_setup: []
must_haves:
  truths:
    - "A revision request creates a new proposal version inside the same workflow run rather than overwriting or forking the run."
    - "Each proposal version preserves lineage to the superseded version and to the revision feedback that produced it."
    - "Approval or rejection decisions always resolve a specific proposal version, and operator views can show which version is latest, approved, rejected, or superseded."
    - "Revision is distinct from retry: revision re-enters the proposal-producing specialist step with feedback, while retry re-attempts the same failed step."
  artifacts:
    - path: "packages/storage/src/helm_storage/repositories/workflow_artifacts.py"
      provides: "Artifact lineage queries for latest proposal version, superseded proposal history, and revision-linked proposal creation."
    - path: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      provides: "Revision-driven re-entry into the proposal-producing specialist step with durable feedback linkage."
    - path: "apps/api/src/helm_api/services/workflow_status_service.py"
      provides: "Latest-first and lineage-aware projection of proposal versions and approval outcomes."
    - path: "apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py"
      provides: "Compact Telegram views that default to the latest proposal while keeping older versions inspectable."
    - path: "docs/runbooks/workflow-runs.md"
      provides: "Manual verification notes for revision, supersession, and approved-version inspection."
  key_links:
    - from: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      to: "packages/storage/src/helm_storage/repositories/workflow_artifacts.py"
      via: "Revision requests create new schedule proposal versions linked to prior artifacts instead of overwriting payloads."
    - from: "apps/api/src/helm_api/services/workflow_status_service.py"
      to: "apps/telegram-bot/src/helm_telegram_bot/services/workflow_status_service.py"
      via: "Both operator surfaces reuse the same latest-first proposal version projection."
    - from: "apps/api/src/helm_api/routers/workflow_runs.py"
      to: "packages/orchestration/src/helm_orchestration/resume_service.py"
      via: "Version-specific decisions and revision resumes flow through the persisted kernel state machine."
---

<objective>
Implement revision-driven proposal versioning and operator-visible decision lineage for approval-driven rework.

Purpose: Phase 2 is incomplete if a revision request only edits the current proposal in place. This plan makes proposal rework durable and inspectable by version so approval history survives later retries, resumes, and downstream sync phases.
Output: Revision payload contracts, proposal-version lineage behavior, latest-first read-model updates, API and Telegram version inspection, runbook notes, and tests for supersession and version-specific decisions.
</objective>

<execution_context>
@/Users/ankush/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ankush/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-specialist-dispatch-and-approval-semantics/02-CONTEXT.md
@.planning/phases/02-specialist-dispatch-and-approval-semantics/02-RESEARCH.md
@.planning/phases/02-specialist-dispatch-and-approval-semantics/02-01-PLAN.md
@.planning/phases/02-specialist-dispatch-and-approval-semantics/02-02-PLAN.md
@apps/api/src/helm_api/services/workflow_status_service.py
@apps/telegram-bot/src/helm_telegram_bot/commands/approve.py
@docs/runbooks/workflow-runs.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement revision-linked proposal version creation in the kernel and repositories</name>
  <files>packages/storage/src/helm_storage/repositories/contracts.py, packages/storage/src/helm_storage/repositories/workflow_artifacts.py, packages/storage/src/helm_storage/repositories/workflow_events.py, packages/orchestration/src/helm_orchestration/schemas.py, packages/orchestration/src/helm_orchestration/workflow_service.py, packages/orchestration/src/helm_orchestration/resume_service.py</files>
  <action>Extend artifact and orchestration contracts so a revision request persists its feedback durably and causes the proposal-producing specialist step to generate a new `schedule_proposal` artifact version. Use existing lineage fields such as `version_number`, `lineage_parent_id`, and `supersedes_artifact_id` to preserve the full proposal history inside the same run. Ensure the new proposal version carries forward the relevant upstream task artifacts and scheduling constraints, while remaining explicitly linked to the revision request artifact or payload that prompted it. Keep retry semantics separate: retry should not create a new proposal version unless the operator asked for revision.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py -k revision` passes.</verify>
  <done>Revision requests produce new persisted proposal versions with explicit supersession and feedback lineage inside the same workflow run.</done>
</task>

<task type="auto">
  <name>Task 2: Make workflow status and operator routes version-aware</name>
  <files>apps/api/src/helm_api/schemas.py, apps/api/src/helm_api/services/workflow_status_service.py, apps/api/src/helm_api/routers/workflow_runs.py, apps/telegram-bot/src/helm_telegram_bot/services/workflow_status_service.py, apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py, apps/telegram-bot/src/helm_telegram_bot/commands/approve.py</files>
  <action>Update the shared read model so the default operator view shows the latest proposal version while preserving queryable history for earlier versions. Include version number, superseded/approved/rejected status, revision feedback summary, and decision linkage for each proposal version. Add API detail fields or routes for proposal-version inspection and ensure decision submission targets a concrete proposal version rather than an implied latest record. Keep Telegram compact: latest-first by default, with a deliberate command path to inspect prior versions when needed.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_status_service.py tests/integration/test_workflow_status_routes.py -k version` passes.</verify>
  <done>Operator surfaces can explain which proposal version is current, which versions were superseded, and which exact version was approved or rejected.</done>
</task>

<task type="auto">
  <name>Task 3: Add revision/versioning tests and runbook verification notes</name>
  <files>docs/runbooks/workflow-runs.md, tests/unit/test_workflow_orchestration_service.py, tests/unit/test_workflow_status_service.py, tests/unit/test_telegram_commands.py, tests/integration/test_workflow_status_routes.py</files>
  <action>Add hermetic tests for multiple revision cycles in the same run, including revision feedback capture, new proposal version creation, superseded lineage, latest-first status projection, and version-specific approve/reject decisions. Cover the case where a later version is approved while an earlier version remains inspectable but no longer actionable. Update the workflow runbook with concise manual verification steps for requesting revision from Telegram or API, confirming a new proposal version appears, inspecting older versions, and verifying the approved version is the one recorded in status/detail output.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py tests/unit/test_workflow_status_service.py tests/unit/test_telegram_commands.py tests/integration/test_workflow_status_routes.py` passes.</verify>
  <done>Tests and operator notes prove revision-driven rework preserves proposal lineage and version-specific decisions clearly.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_status_service.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_telegram_commands.py`
- [ ] `uv run --frozen --extra dev pytest tests/integration/test_workflow_status_routes.py`
- [ ] Requesting revision creates a new schedule proposal version instead of overwriting the prior proposal artifact
- [ ] Proposal lineage records superseded links and revision feedback linkage durably
- [ ] Approval and rejection decisions reference the specific proposal version they resolved
- [ ] API and Telegram status views default to the latest proposal version but preserve inspectable earlier versions
- [ ] Runbook steps document manual verification for revision, supersession, and version-specific approval state
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Revision semantics preserve inspectable proposal history inside one workflow run
- Operators can tell exactly which version is current and which version received the final decision
- Proposal versioning is durable enough that later sync and summary phases can attach side effects and final lineage to the correct approved artifact
  </success_criteria>

<output>
After completion, create `.planning/phases/02-specialist-dispatch-and-approval-semantics/02-03-SUMMARY.md`
</output>
