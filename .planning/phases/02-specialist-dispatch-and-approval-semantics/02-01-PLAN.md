---
phase: 02-specialist-dispatch-and-approval-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/versions/20260313_0008_specialist_dispatch.py
  - packages/storage/src/helm_storage/models.py
  - packages/storage/src/helm_storage/repositories/contracts.py
  - packages/storage/src/helm_storage/repositories/workflow_artifacts.py
  - packages/storage/src/helm_storage/repositories/workflow_steps.py
  - packages/storage/src/helm_storage/repositories/__init__.py
  - packages/orchestration/src/helm_orchestration/contracts.py
  - packages/orchestration/src/helm_orchestration/schemas.py
  - packages/orchestration/src/helm_orchestration/workflow_service.py
  - packages/orchestration/src/helm_orchestration/resume_service.py
  - apps/worker/src/helm_worker/jobs/registry.py
  - apps/worker/src/helm_worker/jobs/workflow_runs.py
  - tests/unit/test_workflow_orchestration_service.py
  - tests/unit/test_workflow_repositories.py
autonomous: true
requirements:
  - AGNT-01
  - AGNT-02
  - AGNT-03
  - ARTF-04
  - DEMO-02
  - DEMO-03
user_setup: []
must_haves:
  truths:
    - "Helm invokes `TaskAgent` and `CalendarAgent` through one typed kernel dispatch contract instead of ad hoc worker wiring."
    - "Each specialist execution writes a durable invocation record with input reference, output reference, timing, and result status."
    - "The representative scheduling flow can convert raw request input into normalized task artifacts, then convert validated tasks into a persisted schedule proposal artifact."
    - "Specialist execution remains inside kernel-owned workflow step semantics so later approval and sync phases can build on the same step history."
  artifacts:
    - path: "packages/orchestration/src/helm_orchestration/schemas.py"
      provides: "Typed specialist envelopes and output payloads for `TaskAgent`, `CalendarAgent`, normalized tasks, and schedule proposals."
    - path: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      provides: "Kernel operations to persist invocation records, store specialist outputs, and advance between specialist steps."
    - path: "packages/orchestration/src/helm_orchestration/resume_service.py"
      provides: "Typed specialist handler registry and workflow-semantic dispatch instead of loose step-name-only execution."
    - path: "packages/storage/src/helm_storage/models.py"
      provides: "Durable specialist invocation schema plus schedule proposal artifact support."
    - path: "tests/unit/test_workflow_orchestration_service.py"
      provides: "Service-level verification for `TaskAgent` then `CalendarAgent` execution and persisted invocation lineage."
  key_links:
    - from: "packages/orchestration/src/helm_orchestration/resume_service.py"
      to: "apps/worker/src/helm_worker/jobs/workflow_runs.py"
      via: "Worker polling resumes runs through the typed specialist dispatch registry."
    - from: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      to: "packages/storage/src/helm_storage/repositories/contracts.py"
      via: "Kernel step execution writes durable specialist invocation and artifact records through repository contracts."
    - from: "packages/orchestration/src/helm_orchestration/schemas.py"
      to: "packages/storage/src/helm_storage/models.py"
      via: "Pydantic specialist payloads and persisted invocation/artifact schemas stay aligned."
---

<objective>
Implement kernel-owned specialist dispatch for `TaskAgent` and `CalendarAgent`, including durable invocation records and schedule proposal persistence.

Purpose: Phase 2 starts by turning the Phase 1 workflow state machine into a typed specialist execution kernel that can drive the representative scheduling flow without yet introducing human approval or downstream writes.
Output: Specialist payload schemas, durable invocation persistence, typed dispatch registration, worker integration, and tests proving request-to-task-to-schedule progression.
</objective>

<execution_context>
@/Users/ankush/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ankush/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-specialist-dispatch-and-approval-semantics/02-CONTEXT.md
@.planning/phases/02-specialist-dispatch-and-approval-semantics/02-RESEARCH.md
@packages/orchestration/src/helm_orchestration/workflow_service.py
@packages/orchestration/src/helm_orchestration/resume_service.py
@packages/orchestration/src/helm_orchestration/schemas.py
@packages/storage/src/helm_storage/models.py
@apps/worker/src/helm_worker/jobs/workflow_runs.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add durable specialist invocation persistence and schedule proposal artifact support</name>
  <files>migrations/versions/20260313_0008_specialist_dispatch.py, packages/storage/src/helm_storage/models.py, packages/storage/src/helm_storage/repositories/contracts.py, packages/storage/src/helm_storage/repositories/workflow_artifacts.py, packages/storage/src/helm_storage/repositories/workflow_steps.py, packages/storage/src/helm_storage/repositories/__init__.py</files>
  <action>Create the durable storage contract for specialist execution. Add either a dedicated `workflow_specialist_invocations` table or an equally explicit persisted model that records run ID, step ID, specialist name, typed input reference, output artifact reference, started/completed timestamps, status, and error summary. Extend artifact/repository contracts so `schedule_proposal` becomes a first-class artifact type with versioning and lineage support rather than an overloaded validation or summary payload. Keep storage boring, queryable, and Postgres-first; do not hide invocation details inside transient worker memory or blob fields that read models cannot project cleanly.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_repositories.py -k specialist` passes.</verify>
  <done>Storage can durably represent specialist invocations and persisted schedule proposal artifacts with explicit linkage to the step and run that produced them.</done>
</task>

<task type="auto">
  <name>Task 2: Define typed specialist contracts and kernel dispatch semantics</name>
  <files>packages/orchestration/src/helm_orchestration/contracts.py, packages/orchestration/src/helm_orchestration/schemas.py, packages/orchestration/src/helm_orchestration/workflow_service.py, packages/orchestration/src/helm_orchestration/resume_service.py</files>
  <action>Add typed input and output envelopes for `TaskAgent` and `CalendarAgent`. `TaskAgent` input must carry raw request text plus workflow metadata and known constraints without over-normalizing the request ahead of the specialist. `CalendarAgent` input must derive from validated normalized task artifacts plus explicit scheduling constraints, with source context passed only as supplemental data. Update workflow execution so specialist steps persist the invocation start, call the registered typed handler, persist the output artifact, record result status and timing, and advance to the next step only after existing validation rules pass. Replace loose step-name-only specialist selection with a registry keyed by workflow semantics such as `(workflow_type, step_name)` or an equivalent workflow-owned definition in `packages/orchestration`.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py -k 'task_agent or calendar_agent or specialist'` passes.</verify>
  <done>The kernel exposes a single typed specialist dispatch contract that can execute both `TaskAgent` and `CalendarAgent` steps and persist their invocation lineage.</done>
</task>

<task type="auto">
  <name>Task 3: Wire worker execution and add representative scheduling-flow coverage</name>
  <files>apps/worker/src/helm_worker/jobs/registry.py, apps/worker/src/helm_worker/jobs/workflow_runs.py, tests/unit/test_workflow_orchestration_service.py, tests/unit/test_workflow_repositories.py</files>
  <action>Register the specialist-capable workflow handlers in the worker path without leaking business logic into app wiring. Add tests that simulate the representative scheduling flow from raw request to normalized task artifact to persisted schedule proposal, asserting that `TaskAgent` execution satisfies `DEMO-02`, `CalendarAgent` execution satisfies `DEMO-03`, and each specialist step produces a durable invocation record satisfying `AGNT-03`. Include coverage for warnings on valid specialist output, for validation failure after specialist output, and for worker restart-safe resumption between the two specialist steps.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py tests/unit/test_worker_registry.py tests/unit/test_workflow_repositories.py` passes.</verify>
  <done>Worker registration and tests prove the Phase 2 specialist path is durable, typed, and restart-safe before approval checkpoints are added.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_repositories.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_worker_registry.py`
- [ ] `TaskAgent` input is typed from raw workflow request and workflow metadata rather than raw dict passing
- [ ] `CalendarAgent` input is typed from validated task artifacts and explicit scheduling constraints
- [ ] Each specialist execution persists input reference, output reference, timing, and result status durably
- [ ] A schedule proposal artifact is stored as an inspectable workflow artifact with lineage
- [ ] Worker restart or resume preserves the correct next specialist step from durable state
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Specialist execution is owned by the kernel, not by loose worker wiring or Telegram/API surfaces
- The representative scheduling flow can reach a persisted schedule proposal artifact without any downstream side effects
- Invocation lineage is durable and queryable enough for later approval views and operator inspection
  </success_criteria>

<output>
After completion, create `.planning/phases/02-specialist-dispatch-and-approval-semantics/02-01-SUMMARY.md`
</output>
