---
phase: 01-durable-workflow-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - apps/api/src/helm_api/schemas.py
  - apps/api/src/helm_api/routers/workflow_runs.py
  - apps/api/src/helm_api/services/workflow_status_service.py
  - apps/api/src/helm_api/main.py
  - apps/telegram-bot/src/helm_telegram_bot/services/workflow_status_service.py
  - apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py
  - apps/telegram-bot/src/helm_telegram_bot/main.py
  - docs/runbooks/workflow-runs.md
  - tests/unit/test_workflow_status_service.py
  - tests/integration/test_workflow_status_routes.py
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-04
  - ARTF-05
  - AGNT-06
user_setup: []
must_haves:
  truths:
    - "An operator can start a workflow run from a new request through an existing Helm surface and receive the created run ID, initial status, and current step without querying storage directly."
    - "An operator can inspect workflow run status, current step, latest outcome, and needs-action state without querying the database directly."
    - "API detail views expose lineage across raw request, step transitions, validation outcomes, artifacts, and final state for a single workflow run."
    - "Telegram-friendly workflow summaries stay concise while still showing what run this is, what happened last, whether action is required, and whether retry or terminate is the next recovery action."
    - "Operator-facing read models distinguish ordinary execution failures from blocked validation failures, expose an explicit `paused_state` plus nullable `pause_reason`, and expose the final summary contract even when approval or downstream sync linkages are still null."
  artifacts:
    - path: "apps/api/src/helm_api/routers/workflow_runs.py"
      provides: "Workflow ingest plus inspection endpoints for run creation, run list, and detail inspection."
    - path: "apps/api/src/helm_api/services/workflow_status_service.py"
      provides: "Triage-oriented read model built from durable workflow tables, including explicit paused-state fields."
    - path: "apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py"
      provides: "Telegram command entrypoints for starting workflows, concise workflow run summaries, and recovery actions."
    - path: "tests/unit/test_workflow_status_service.py"
      provides: "Read-model coverage for workflow status and lineage formatting."
    - path: "tests/integration/test_workflow_status_routes.py"
      provides: "Route coverage for workflow inspection endpoints."
    - path: "docs/runbooks/workflow-runs.md"
      provides: "Operator runbook covering workflow inspection plus manual retry/terminate verification notes for API and Telegram surfaces."
  key_links:
    - from: "apps/api/src/helm_api/routers/workflow_runs.py"
      to: "apps/api/src/helm_api/services/workflow_status_service.py"
      via: "API routes delegate workflow inspection to a single read-model service."
    - from: "apps/api/src/helm_api/services/workflow_status_service.py"
      to: "packages/storage/src/helm_storage/repositories/workflow_runs.py"
      via: "Operator status view is sourced from durable workflow run and artifact records."
    - from: "apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py"
      to: "apps/telegram-bot/src/helm_telegram_bot/services/workflow_status_service.py"
      via: "Telegram command formatting reuses the workflow read model rather than duplicating status logic."
---

<objective>
Expose durable workflow ingest and inspection through API and Telegram-friendly operator paths so users can start, inspect, and recover workflow runs without raw database access.

Purpose: The Phase 1 goal requires an explicit request-ingest path plus inspectable run status and lineage, not just persisted records. This plan turns the new kernel state into operator-facing API and Telegram flows for creation, triage, and recovery.
Output: Workflow run API schemas/routes/services, Telegram status service and commands, and route/read-model tests for start, status, lineage, and recovery visibility.
</objective>

<execution_context>
@/Users/ankush/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ankush/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-durable-workflow-foundation/01-CONTEXT.md
@.planning/phases/01-durable-workflow-foundation/01-RESEARCH.md
@.planning/phases/01-durable-workflow-foundation/01-VALIDATION.md
@apps/api/src/helm_api/main.py
@apps/api/src/helm_api/routers/status.py
@apps/api/src/helm_api/services/status_service.py
@apps/api/src/helm_api/services/artifact_trace_service.py
@apps/telegram-bot/src/helm_telegram_bot/services/command_service.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build a single workflow status read model for operator triage</name>
  <files>apps/api/src/helm_api/services/workflow_status_service.py, apps/api/src/helm_api/schemas.py, tests/unit/test_workflow_status_service.py</files>
  <action>Implement a workflow status service that projects durable run, step, artifact, validation, and ordinary execution failure records into list and detail payloads optimized for triage. Include run identity, workflow type, current step, run status, explicit `paused_state`, nullable `pause_reason`, needs-action flag, last event summary, failure summary, retryability, failure kind, latest validation outcome, and a lineage section linking raw request, intermediate artifacts, final summary, and step transitions. The final summary projection must expose stable fields for request linkage, specialist output linkage, nullable approval-decision linkage, and nullable downstream-sync linkage so operator clients can adopt the Phase 1 contract before later phases populate those relationships. Keep this logic in one read-model service so API and Telegram surfaces do not diverge.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_status_service.py` passes.</verify>
  <done>The workflow read model can answer the phase triage question from persisted state only: what run is this, what step is it on, is it paused and why, what happened last, and does it need action now.</done>
</task>

<task type="auto">
  <name>Task 2: Add API ingest, inspection, and blocked-run action routes backed by the workflow read model</name>
  <files>apps/api/src/helm_api/routers/workflow_runs.py, apps/api/src/helm_api/main.py, apps/api/src/helm_api/schemas.py, tests/integration/test_workflow_status_routes.py</files>
  <action>Add dedicated workflow endpoints under the existing API surface for creating a run from a new request, returning the created run ID plus initial status/current step, run listing, needs-action filtering, run detail with artifact and validation lineage, and explicit blocked-run or failed-run retry/terminate actions. Wire the routes into `main.py` using the existing thin-router pattern. Ensure route contracts surface blocked validation failures, ordinary execution failures, explicit paused-state fields, retryability, available next actions, and final summaries explicitly rather than forcing clients to infer them from generic status strings. Add integration tests for create-from-request, empty, running, blocked, retried, terminated, failed, and completed cases, including a failed-step case that never produced a validation artifact.</action>
  <verify>`uv run --frozen --extra dev pytest tests/integration/test_workflow_status_routes.py tests/unit/test_api_status.py` passes.</verify>
  <done>Operators can start a run from a new request and inspect workflow status and lineage through API routes, then explicitly retry or terminate blocked validation-failure runs without touching raw DB tables.</done>
</task>

<task type="auto">
  <name>Task 3: Add Telegram-friendly workflow start, summary, and blocked-run recovery commands</name>
  <files>apps/telegram-bot/src/helm_telegram_bot/services/workflow_status_service.py, apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py, apps/telegram-bot/src/helm_telegram_bot/main.py, docs/runbooks/workflow-runs.md, tests/unit/test_telegram_commands.py</files>
  <action>Add a Telegram path that reuses the workflow status projection and supports starting a workflow from a new request through an existing bot command, returning the created run ID plus initial status/current step, alongside concise summaries for recent runs and needs-action runs. Add explicit recovery commands for retrying or terminating blocked validation-failure runs and ordinarily failed retryable runs, keeping the bot limited to operator-requested creation, inspection, and recovery actions in this phase. Format messages to be brief but explicit about run ID, current step, explicit paused-state information, last outcome, whether the failure is blocked or failed, and whether retry or termination is the next operator action. Update the runbook with manual verification notes for API and Telegram start plus recovery flows.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_telegram_commands.py tests/unit/test_workflow_status_service.py` passes.</verify>
  <done>Telegram exposes lightweight, triage-first workflow creation and visibility sourced from the same durable read model as the API and supports explicit retry/terminate recovery for blocked runs.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_status_service.py`
- [ ] `uv run --frozen --extra dev pytest tests/integration/test_workflow_status_routes.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_telegram_commands.py`
- [ ] API and Telegram entrypoints both allow a user to start a workflow run from a new request and return run ID plus initial status/current step through an existing Helm surface
- [ ] API and Telegram views both expose current step, latest outcome, explicit `paused_state`, nullable `pause_reason`, needs-action state, and lineage without duplicating independent status rules
- [ ] API and Telegram recovery paths both expose explicit retry/terminate actions for blocked validation failures and ordinary retryable execution failures
- [ ] Final summary read contracts expose nullable approval-decision and downstream-sync linkage fields without clients inferring missing keys
- [ ] `docs/runbooks/workflow-runs.md` documents operator-facing inspection and recovery checks
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- API and Telegram operator surfaces expose request ingest, inspectable run status, validation failures, and artifact lineage sourced from the durable workflow kernel
- A blocked or failed run clearly shows what happened last, whether operator attention is needed now, and which explicit recovery actions are available
- Paused or approval-gated conditions are represented explicitly in read models via `paused_state` and nullable `pause_reason`, not inferred indirectly from generic status text
- Operator read models expose the stable final summary shape before later approval and sync phases populate those links
  </success_criteria>

<output>
After completion, create `.planning/phases/01-durable-workflow-foundation/01-03-SUMMARY.md`
</output>
