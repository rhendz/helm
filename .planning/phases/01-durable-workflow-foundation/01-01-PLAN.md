---
phase: 01-durable-workflow-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/versions/20260313_0007_workflow_foundation.py
  - packages/storage/src/helm_storage/models.py
  - packages/storage/src/helm_storage/repositories/contracts.py
  - packages/storage/src/helm_storage/repositories/workflow_runs.py
  - packages/storage/src/helm_storage/repositories/workflow_steps.py
  - packages/storage/src/helm_storage/repositories/workflow_artifacts.py
  - packages/storage/src/helm_storage/repositories/workflow_events.py
  - packages/storage/src/helm_storage/repositories/__init__.py
  - tests/unit/test_workflow_repositories.py
autonomous: true
requirements:
  - FLOW-01
  - FLOW-03
  - ARTF-01
  - ARTF-02
  - ARTF-03
  - ARTF-05
user_setup: []
must_haves:
  truths:
    - "A workflow request survives process restart because its run identity, current step, and status are durably stored in Postgres."
    - "An operator-facing status surface can later reconstruct what happened last from persisted step attempts, artifacts, and run state alone."
    - "Each workflow run preserves inspectable lineage from raw request through validation and final summary artifacts without relying on prompt memory."
  artifacts:
    - path: "packages/storage/src/helm_storage/models.py"
      provides: "Workflow run, step, artifact, and event ORM models with explicit durable state fields."
      contains: "workflow_runs"
    - path: "packages/storage/src/helm_storage/repositories/contracts.py"
      provides: "Typed dataclasses and repository protocols for workflow foundation persistence."
    - path: "packages/storage/src/helm_storage/repositories/workflow_runs.py"
      provides: "Run repository queries for create, update, lookup, and resume-safe reads."
    - path: "packages/storage/src/helm_storage/repositories/workflow_artifacts.py"
      provides: "Artifact persistence with typed payloads, versioning, and lineage links."
    - path: "tests/unit/test_workflow_repositories.py"
      provides: "Repository-level verification for workflow foundation persistence behavior."
  key_links:
    - from: "migrations/versions/20260313_0007_workflow_foundation.py"
      to: "packages/storage/src/helm_storage/models.py"
      via: "Schema alignment between migration and ORM definitions."
    - from: "packages/storage/src/helm_storage/repositories/workflow_runs.py"
      to: "packages/storage/src/helm_storage/models.py"
      via: "SQLAlchemy repository operations over workflow run tables."
    - from: "packages/storage/src/helm_storage/repositories/workflow_artifacts.py"
      to: "packages/storage/src/helm_storage/repositories/contracts.py"
      via: "Typed artifact creation and lineage query contracts."
---

<objective>
Establish the durable Postgres persistence layer for workflow runs, step attempts, artifacts, and transition history.

Purpose: Create the kernel-owned workflow vocabulary and storage contracts that later orchestration and operator surfaces can rely on without reusing email-specific tables.
Output: Migration, ORM models, repository contracts, repository implementations, and repository tests for workflow foundation entities.
</objective>

<execution_context>
@/Users/ankush/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ankush/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-durable-workflow-foundation/01-CONTEXT.md
@.planning/phases/01-durable-workflow-foundation/01-RESEARCH.md
@packages/storage/src/helm_storage/models.py
@packages/storage/src/helm_storage/repositories/contracts.py
@packages/storage/README.md
@docs/runbooks/migrations.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add workflow foundation schema and ORM models</name>
  <files>migrations/versions/20260313_0007_workflow_foundation.py, packages/storage/src/helm_storage/models.py</files>
  <action>Create new workflow-native tables for runs, steps, artifacts, and durable transition history. Keep these tables separate from `agent_runs` and legacy email-specific artifacts. Include explicit run status, current step, needs-action flag, attempt counters, validation outcome summary, execution error summary, failure class, and retryability state so ordinary step-execution failures can be durably distinguished from `validation_failed` blocked states. Also include artifact type, schema version, version number, producer step, and lineage references so Phase 1 storage covers raw input through final summary. The final workflow summary artifact contract must be established in storage now with explicit linkage slots for request artifact, intermediate specialist outputs, validation artifacts, nullable approval-decision fields, and nullable downstream-sync linkage fields so later phases extend the same schema instead of replacing it. Preserve boring SQLAlchemy patterns already used in storage.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_repositories.py` passes with schema creation exercising the new workflow tables.</verify>
  <done>Migration and ORM definitions exist, align on field names and relationships, and represent the durable vocabulary described in the phase research.</done>
</task>

<task type="auto">
  <name>Task 2: Implement typed repository contracts and SQLAlchemy repositories</name>
  <files>packages/storage/src/helm_storage/repositories/contracts.py, packages/storage/src/helm_storage/repositories/workflow_runs.py, packages/storage/src/helm_storage/repositories/workflow_steps.py, packages/storage/src/helm_storage/repositories/workflow_artifacts.py, packages/storage/src/helm_storage/repositories/workflow_events.py, packages/storage/src/helm_storage/repositories/__init__.py</files>
  <action>Add explicit dataclasses and protocols for creating runs, recording step attempts, storing versioned artifacts, recording validation results as durable artifacts or linked records, recording ordinary execution failures on a step attempt, and querying latest runnable or inspectable state. Repository methods must support restart-safe reads such as "get run with current step and latest artifacts", "get last failed step with retryability", and "list runs needing action" without depending on in-memory orchestration state. Define the final workflow summary artifact payload as a typed contract now, including nullable or empty approval-decision fields and downstream-sync linkage fields, so API and Telegram read paths can depend on a stable summary schema before approval or sync features exist.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_repositories.py tests/unit/test_storage_repositories.py` passes.</verify>
  <done>Storage exposes boring, typed repository interfaces that can create and read workflow foundation records without boundary leakage into API or worker code.</done>
</task>

<task type="auto">
  <name>Task 3: Add hermetic repository coverage for workflow persistence invariants</name>
  <files>tests/unit/test_workflow_repositories.py</files>
  <action>Add repository tests covering run creation, step attempt sequencing, artifact versioning, raw request persistence, normalized task artifact persistence, validation artifact persistence, ordinary execution failure persistence, final summary persistence, and resume-safe queries after simulated interruption. Assert that an execution failure stores the failed step, operator-readable error summary, and retryability state durably without being conflated with validation-blocked status. Include assertions for final summary lineage links plus nullable approval-decision and downstream-sync linkage fields so later API inspection can be built on a tested future-compatible data shape.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_repositories.py` passes.</verify>
  <done>Tests prove the storage layer satisfies FLOW-01, FLOW-03, ARTF-01, ARTF-02, ARTF-03, and ARTF-05 at the repository boundary.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_repositories.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_storage_repositories.py`
- [ ] The migration and ORM definitions agree on workflow run, step, artifact, and event table names and relationships.
- [ ] Repository queries can reconstruct current workflow status from persisted state only.
- [ ] Repository contracts and tests distinguish ordinary execution failures from blocked validation failures.
- [ ] Final workflow summary storage contract includes nullable or empty approval-decision and downstream-sync linkage fields.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Workflow foundation tables and repositories establish the durable kernel vocabulary without mutating legacy email-specific persistence into the new core model
- Persisted records cover raw request, normalized artifacts, validation lineage, ordinary execution failure details, final summary, and resume-safe current-step status
- The final summary artifact shape is future-compatible with approval and downstream sync linkage without Phase 1 inventing those behaviors
  </success_criteria>

<output>
After completion, create `.planning/phases/01-durable-workflow-foundation/01-01-SUMMARY.md`
</output>
