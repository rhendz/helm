---
phase: 01-durable-workflow-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - packages/orchestration/src/helm_orchestration/__init__.py
  - packages/orchestration/src/helm_orchestration/contracts.py
  - packages/orchestration/src/helm_orchestration/schemas.py
  - packages/orchestration/src/helm_orchestration/validators.py
  - packages/orchestration/src/helm_orchestration/workflow_service.py
  - packages/orchestration/src/helm_orchestration/resume_service.py
  - apps/worker/src/helm_worker/jobs/registry.py
  - apps/worker/src/helm_worker/jobs/workflow_runs.py
  - packages/orchestration/README.md
  - tests/unit/test_workflow_orchestration_service.py
autonomous: true
requirements:
  - FLOW-03
  - FLOW-04
  - AGNT-04
  - AGNT-05
  - AGNT-06
  - ARTF-03
  - ARTF-05
user_setup: []
must_haves:
  truths:
    - "Workflow step execution only advances after typed artifact validation passes or passes with warnings."
    - "Malformed, incomplete, or materially ambiguous specialist output moves the step into `validation_failed` and the run into a durable blocked state."
    - "Blocked validation-failure runs require an explicit persisted retry or terminate action before any downstream step can continue."
    - "An ordinary step-execution exception or adapter-free execution error marks the step and run as failed durably, with the failed step, error summary, and retryability state persisted even when no validation artifact is produced."
    - "Failure details and retryability are stored durably so worker restart can resume, retry, or terminate from the correct persisted step boundary."
  artifacts:
    - path: "packages/orchestration/src/helm_orchestration/schemas.py"
      provides: "Typed workflow request and artifact schemas for raw input, normalized tasks, validation reports, and final summaries."
    - path: "packages/orchestration/src/helm_orchestration/validators.py"
      provides: "Explicit validation boundary returning pass, pass-with-warning, or failure outcomes."
    - path: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      provides: "Run creation, step transition, artifact persistence, and blocked-state orchestration."
    - path: "apps/worker/src/helm_worker/jobs/workflow_runs.py"
      provides: "Worker entrypoint that claims runnable workflow work from persisted state and ignores blocked runs until explicit retry unblocks them."
    - path: "packages/orchestration/README.md"
      provides: "Operator/developer notes describing blocked-run retry and terminate semantics plus worker resume behavior."
    - path: "tests/unit/test_workflow_orchestration_service.py"
      provides: "Service-level verification for validation gating and failure persistence."
  key_links:
    - from: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      to: "packages/storage/src/helm_storage/repositories/workflow_runs.py"
      via: "Durable state transitions are stored through repository contracts."
    - from: "packages/orchestration/src/helm_orchestration/validators.py"
      to: "packages/orchestration/src/helm_orchestration/schemas.py"
      via: "Typed validation against workflow artifact schemas."
    - from: "apps/worker/src/helm_worker/jobs/workflow_runs.py"
      to: "packages/orchestration/src/helm_orchestration/resume_service.py"
      via: "Restart-safe worker polling derives next runnable step from persisted workflow state."
---

<objective>
Implement the typed orchestration and validation boundary that turns persisted workflow records into a durable, restart-safe workflow state machine.

Purpose: Phase 1 is not complete with storage alone; the kernel must own step advancement, validation failure blocking, and persisted failure/retryability semantics before later phases add approvals or side effects.
Output: Typed workflow schemas, validation services, orchestration services, worker polling/resume entrypoint, and service-level tests for success and blocked-state transitions.
</objective>

<execution_context>
@/Users/ankush/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ankush/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-durable-workflow-foundation/01-CONTEXT.md
@.planning/phases/01-durable-workflow-foundation/01-RESEARCH.md
@.planning/phases/01-durable-workflow-foundation/01-VALIDATION.md
@.planning/phases/01-durable-workflow-foundation/01-01-PLAN.md
@packages/orchestration/README.md
@apps/worker/src/helm_worker/jobs/registry.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define typed workflow schemas and validation outcomes</name>
  <files>packages/orchestration/src/helm_orchestration/contracts.py, packages/orchestration/src/helm_orchestration/schemas.py, packages/orchestration/src/helm_orchestration/validators.py, packages/orchestration/src/helm_orchestration/__init__.py</files>
  <action>Create Pydantic-backed request and artifact schemas for raw workflow input, normalized task artifacts, validation result payloads, execution failure payloads, and final workflow summary artifacts. Implement a small validator registry keyed by step name or artifact type. Validation results must distinguish `passed`, `passed_with_warnings`, and `failed`, preserve machine-readable issues plus operator-readable summaries, and avoid generic plugin abstractions beyond what this phase needs. The final summary schema must explicitly include nullable or empty approval-decision fields and downstream-sync linkage fields so the Phase 1 artifact contract is stable before later phases populate those links.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py -k validation` passes.</verify>
  <done>Typed schema contracts exist and validation outcomes can represent blocking failures plus non-blocking warnings in a durable form.</done>
</task>

<task type="auto">
  <name>Task 2: Implement orchestration services for run creation, step transitions, and blocked validation failures</name>
  <files>packages/orchestration/src/helm_orchestration/workflow_service.py, packages/orchestration/src/helm_orchestration/resume_service.py</files>
  <action>Build orchestration services that create a run, start a step attempt, persist candidate artifacts, validate them, and commit the resulting run and step state atomically. On validation failure, record failure summary and retryability, set step status to `validation_failed`, set run status to `blocked`, and persist the state needed for downstream read models to expose explicit `paused_state` plus nullable `pause_reason` rather than inferring blocked state indirectly. On ordinary execution failure before or outside validation, record the failed step, operator-readable error summary, machine-readable failure payload, and retryability state durably, set step status to `failed`, set run status to `failed`, and keep the run inspectable for later retry or terminal handling without inventing Phase 2 approval semantics. Add explicit service operations for retrying the blocked or failed step when retryable, or terminating the run, each recording a durable transition event and updating runnable state deterministically. On success or warning-only success, advance the run and store the validation artifact lineage needed by operator read paths.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py tests/unit/test_agent_run_lifecycle.py` passes.</verify>
  <done>Orchestration enforces AGNT-04, AGNT-05, AGNT-06, and FLOW-04 using persisted state instead of in-memory step tracking, including distinct durable handling for blocked validation failures versus ordinary execution failures and explicit retry or terminate actions at the persisted step boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Add worker resume entrypoint, blocked-run action handling, and execution notes</name>
  <files>apps/worker/src/helm_worker/jobs/workflow_runs.py, apps/worker/src/helm_worker/jobs/registry.py, packages/orchestration/README.md, tests/unit/test_workflow_orchestration_service.py</files>
  <action>Add a worker job that polls for runnable workflow runs or steps from storage and calls the orchestration resume path. Register it in the worker job registry without entangling existing domain jobs. Ensure blocked runs remain non-runnable until a persisted retry action requeues them or a terminate action closes them, and ensure ordinarily failed runs are not silently resumed until retryability is checked against persisted failure state. Extend tests to simulate interruption, restart, validation failure blocking, ordinary execution failure persistence, explicit retry, explicit terminate, and final summary artifact creation so the next runnable step is derived entirely from persisted records. Update package-level docs with concise manual verification notes for blocked-run and failed-run recovery behavior.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py tests/unit/test_worker_registry.py` passes.</verify>
  <done>Worker polling and orchestration service tests prove a blocked or interrupted workflow can be resumed from durable state, with retry and terminate actions controlling whether blocked runs become runnable again.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_worker_registry.py`
- [ ] Validation-failure flows persist blocked state, failure summary, and retryability without advancing downstream steps
- [ ] Ordinary execution failures persist failed step, error summary, and retryability without being collapsed into validation-blocked semantics
- [ ] Explicit retry requeues only the blocked or failed step and explicit terminate closes the run without downstream advancement
- [ ] Resume logic reconstructs the next runnable step from storage-backed state after simulated interruption
- [ ] `packages/orchestration/README.md` documents blocked-run recovery semantics and manual verification notes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Typed workflow orchestration enforces validation boundaries and durable blocked-state handling before any later-phase approval or adapter semantics
- Failure and retryability information is persisted strongly enough that restart and triage paths do not depend on transient worker memory
- Operators have explicit durable retry and terminate actions for validation-failure and ordinary execution-failure runs instead of relying on implicit worker behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/01-durable-workflow-foundation/01-02-SUMMARY.md`
</output>
