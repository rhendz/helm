---
phase: 04-representative-scheduling-workflow
plan: 03
type: execute
gap_closure: true
wave: 3
depends_on: []
files_modified:
  - apps/api/src/helm_api/services/workflow_status_service.py
  - tests/unit/test_replay_service.py
  - tests/unit/test_workflow_status_service.py
  - tests/unit/test_telegram_commands.py
autonomous: true
requirements:
  - DEMO-06
user_setup: []
must_haves:
  truths:
    - "A representative run that later becomes replay-requested must project the live recovery state even when a successful final-summary artifact still exists from the prior completed execution."
    - "Shared status precedence must come from durable live recovery signals before stale outcome messaging so API and Telegram operator surfaces stay truthful."
    - "Replay-requested summaries must preserve inspectable completion lineage while clearly signaling that downstream follow-up is now pending operator attention."
    - "The fix must stay in the shared projection path rather than adding Telegram-only branching or mutating previously persisted completion artifacts."
  artifacts:
    - path: "apps/api/src/helm_api/services/workflow_status_service.py"
      provides: "Recovery-aware status precedence that lets replay-requested representative runs override stale completed-success summaries."
    - path: "tests/unit/test_workflow_status_service.py"
      provides: "Regression coverage for completed representative runs that later project replay-requested recovery state."
    - path: "tests/unit/test_replay_service.py"
      provides: "Coverage that replaying a completed representative run leaves durable recovery truth available to the shared status projection."
    - path: "tests/unit/test_telegram_commands.py"
      provides: "Coverage that `/workflows` renders replay-requested representative runs as recovery-oriented rather than ordinary completed runs."
  key_links:
    - from: "tests/unit/test_replay_service.py"
      to: "apps/api/src/helm_api/services/workflow_status_service.py"
      via: "Replay state set by the replay path must be the signal the shared projection prefers over stale completion success."
    - from: "apps/api/src/helm_api/services/workflow_status_service.py"
      to: "tests/unit/test_telegram_commands.py"
      via: "Telegram `/workflows` output must stay a thin formatter over the shared recovery-aware status projection."
---

<objective>
Close the diagnosed Phase 4 replay-summary gap without replanning the representative workflow.

Purpose: Make completed-then-replayed representative runs render truthfully as replay-requested recovery work by fixing shared status precedence and adding the minimum regression coverage around replay, status projection, and Telegram summaries.
Output: One focused projection fix in the shared status service plus targeted tests that prove stale final-summary success no longer masks live replay recovery state.
</objective>

<execution_context>
@/Users/ankush/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ankush/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-representative-scheduling-workflow/04-UAT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/debug/phase-04-replay-recovery-summary-gap.md
@apps/api/src/helm_api/services/workflow_status_service.py
@tests/unit/test_replay_service.py
@tests/unit/test_workflow_status_service.py
@tests/unit/test_telegram_commands.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Make live replay recovery override stale completion success in the shared projection</name>
  <files>apps/api/src/helm_api/services/workflow_status_service.py</files>
  <action>Change the representative status projection so replay-requested or equivalent live recovery classification wins over a previously persisted successful final summary when computing the top-level headline, status bucket, downstream sync summary, and attention text. Keep the final-summary artifact intact for lineage details, but do not let it decide the operator-facing state once the run has re-entered recovery. Do not add bot-only conditionals and do not rewrite persistence just to paper over the read-model bug.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_status_service.py -k 'replay_requested or recovery'` passes.</verify>
  <done>Shared API-facing status for a completed representative run that later becomes replay-requested is recovery-oriented rather than ordinary completed.</done>
</task>

<task type="auto">
  <name>Task 2: Add replay and shared-status regressions for completed-then-replayed representative runs</name>
  <files>tests/unit/test_replay_service.py, tests/unit/test_workflow_status_service.py</files>
  <action>Add focused regressions that create a representative run which first completes with a final summary and then enters replay-requested recovery state. Assert that replay leaves the durable recovery signals needed by the projection, and assert that the shared status service now reports replay-requested messaging, live downstream follow-up state, and attention items instead of stale completed-success copy. Keep fixtures hermetic and scoped to this precedence bug rather than broadening the representative workflow matrix again.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_replay_service.py tests/unit/test_workflow_status_service.py -k 'weekly_scheduling or replay_requested or recovery'` passes.</verify>
  <done>The replay path and shared status projection are both pinned by regression tests for the exact completed-then-replayed representative scenario from UAT.</done>
</task>

<task type="auto">
  <name>Task 3: Prove Telegram `/workflows` stays aligned with the shared replay-aware projection</name>
  <files>tests/unit/test_telegram_commands.py</files>
  <action>Add or update Telegram command coverage so `/workflows` for a completed representative run that later becomes replay-requested renders recovery-oriented text, not ordinary completion text. Prefer asserting on the shared projection output already consumed by the command rather than introducing Telegram-specific branching. If a tiny command formatting adjustment is required to expose the corrected recovery wording, keep it minimal and derived from the shared status model.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_telegram_commands.py -k 'workflows and replay'` passes.</verify>
  <done>Telegram operator output matches the corrected shared projection for replay-requested representative runs.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_status_service.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_replay_service.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_telegram_commands.py`
- [ ] A completed representative run that later becomes replay-requested no longer renders as an ordinary completed run through the shared status projection.
- [ ] Live replay or recovery classification overrides stale final-summary success in the operator-facing headline and downstream follow-up summary.
- [ ] Telegram `/workflows` renders the same replay-requested truth exposed by the shared status service.
- [ ] The fix does not rewrite existing final-summary artifacts or introduce Telegram-only status semantics.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The diagnosed Phase 4 replay-summary gap is closed through a shared read-model fix, not a surface-specific workaround
- Representative completion lineage remains inspectable while operator-facing status truthfully reflects live replay recovery state
  </success_criteria>

<output>
After completion, create `.planning/phases/04-representative-scheduling-workflow/04-03-SUMMARY.md`
</output>
