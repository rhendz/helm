---
phase: 04-representative-scheduling-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/helm_api/schemas.py
  - apps/api/src/helm_api/services/workflow_status_service.py
  - apps/telegram-bot/src/helm_telegram_bot/services/workflow_status_service.py
  - apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py
  - apps/worker/src/helm_worker/jobs/workflow_runs.py
  - packages/orchestration/src/helm_orchestration/schemas.py
  - packages/orchestration/src/helm_orchestration/workflow_service.py
  - tests/unit/test_workflow_orchestration_service.py
  - tests/unit/test_workflow_status_service.py
  - tests/unit/test_telegram_commands.py
  - tests/integration/test_workflow_status_routes.py
autonomous: true
requirements:
  - DEMO-01
  - DEMO-04
  - DEMO-05
  - DEMO-06
user_setup: []
must_haves:
  truths:
    - "The representative workflow starts as `weekly_scheduling` from the Telegram-first create path and persists the raw weekly brief as the durable source of truth for later steps."
    - "Request parsing and representative normalization stay shared across Telegram and API create flows instead of living only inside one app-layer command handler."
    - "Schedule proposals remain anchored to persisted task artifacts and still pause at the existing approval checkpoint before any task-system or calendar-system side effect runs."
    - "Revision feedback regenerates a new schedule proposal version for the same run without introducing a demo-only workflow path or bypassing the shared kernel semantics."
  artifacts:
    - path: "apps/telegram-bot/src/helm_telegram_bot/services/workflow_status_service.py"
      provides: "Telegram-first run creation wired to the representative `weekly_scheduling` workflow and shared request contract."
    - path: "apps/worker/src/helm_worker/jobs/workflow_runs.py"
      provides: "Representative task and calendar specialist handlers that transform persisted weekly-request input into real normalized tasks and scheduling proposals."
    - path: "packages/orchestration/src/helm_orchestration/schemas.py"
      provides: "Typed weekly-request and proposal payload fields for explicit constraints, assumptions, rationale, and carry-forward visibility."
    - path: "apps/api/src/helm_api/services/workflow_status_service.py"
      provides: "Shared run creation and status projection behavior for the representative workflow across API and Telegram surfaces."
    - path: "tests/unit/test_workflow_orchestration_service.py"
      provides: "Coverage for representative request parsing, approval pause semantics, and revision-driven proposal regeneration."
  key_links:
    - from: "apps/telegram-bot/src/helm_telegram_bot/services/workflow_status_service.py"
      to: "apps/api/src/helm_api/services/workflow_status_service.py"
      via: "Telegram start delegates representative run creation through the shared workflow status service contract instead of inventing bot-only request semantics."
    - from: "apps/api/src/helm_api/schemas.py"
      to: "packages/orchestration/src/helm_orchestration/schemas.py"
      via: "API input and response payloads mirror the durable weekly-request and proposal contracts used by the kernel."
    - from: "apps/worker/src/helm_worker/jobs/workflow_runs.py"
      to: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      via: "Representative specialist outputs continue to flow through the shared orchestration service so approval checkpoints, revisions, and sync preparation remain kernel-owned."
---

<objective>
Implement the real representative weekly scheduling workflow on top of the existing kernel primitives.

Purpose: Replace the current demo stub with a Telegram-first, DB-first weekly planning flow that starts a `weekly_scheduling` run, normalizes a structured weekly brief into durable task artifacts, generates an honest schedule proposal, and reuses the existing approval and revision path without any dashboard-only or demo-only control plane.
Output: Shared weekly-request contract, corrected start flow, representative task and calendar specialist behavior, compact proposal read-model updates, and tests proving the fixed weekly flow reaches approval and revision safely.
</objective>

<execution_context>
@/Users/ankush/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ankush/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-representative-scheduling-workflow/04-CONTEXT.md
@.planning/phases/04-representative-scheduling-workflow/04-RESEARCH.md
@.planning/phases/04-representative-scheduling-workflow/04-VALIDATION.md
@docs/internal/helm-v1.md
@apps/worker/src/helm_worker/jobs/workflow_runs.py
@apps/api/src/helm_api/services/workflow_status_service.py
@apps/telegram-bot/src/helm_telegram_bot/services/workflow_status_service.py
@packages/orchestration/src/helm_orchestration/schemas.py
@packages/orchestration/src/helm_orchestration/workflow_service.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Align representative run creation and weekly-request contract across Telegram and API</name>
  <files>apps/api/src/helm_api/schemas.py, apps/api/src/helm_api/services/workflow_status_service.py, apps/telegram-bot/src/helm_telegram_bot/services/workflow_status_service.py, apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py, packages/orchestration/src/helm_orchestration/schemas.py</files>
  <action>Correct the representative create path so Telegram start creates `workflow_type=&quot;weekly_scheduling&quot;` at the real first step used by the worker registry. Define a minimal typed weekly-request contract that preserves the original request text while extracting durable metadata for inline tasks, deadlines, priorities, protected time, and no-meeting windows. Keep the app layer thin: Telegram and API create paths should both use the same shared payload shape and validation rules instead of one-off parsing logic. Preserve usable-but-incomplete requests by storing ambiguity or assumption warnings rather than rejecting the run when the brief is imperfect.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_status_service.py tests/unit/test_telegram_commands.py -k workflow_start` and `uv run --frozen --extra dev pytest tests/integration/test_workflow_status_routes.py -k 'create and weekly_scheduling'` pass.</verify>
  <done>The representative run can be created from Telegram-style and API input with the same durable `weekly_scheduling` request contract, the same first-step semantics, and no remaining `weekly_digest` default on the primary start path.</done>
</task>

<task type="auto">
  <name>Task 2: Replace placeholder specialist handlers with request-driven normalization and proposal generation</name>
  <files>apps/worker/src/helm_worker/jobs/workflow_runs.py, packages/orchestration/src/helm_orchestration/schemas.py, packages/orchestration/src/helm_orchestration/workflow_service.py, tests/unit/test_workflow_orchestration_service.py</files>
  <action>Remove the hard-coded task and calendar outputs in the worker job and implement representative logic that reads the persisted weekly brief, produces normalized task artifacts with warnings when fields are missing, and generates a compact but truthful schedule proposal from those tasks and constraints. If the current proposal schema cannot clearly represent carry-forward work, rationale, or assumptions, extend it now in the shared orchestration schema rather than hiding acceptance-critical details inside ad hoc text. Keep scheduling deterministic and hermetic for tests; do not pull from a live backlog, add dashboard scope, or introduce uncontrolled outbound behavior.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py -k weekly_scheduling` passes.</verify>
  <done>The weekly scheduling specialist path is driven by the request artifact instead of fixed demo data, and proposal payloads surface the key facts needed for approval and revision.</done>
</task>

<task type="auto">
  <name>Task 3: Reuse shared approval and revision projections for the representative operator flow</name>
  <files>apps/api/src/helm_api/services/workflow_status_service.py, apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py, tests/unit/test_workflow_status_service.py, tests/unit/test_telegram_commands.py, tests/integration/test_workflow_status_routes.py</files>
  <action>Update the shared status projection and Telegram formatting so the representative proposal view stays compact but still shows scheduled blocks, honored constraints, assumptions, carry-forward items, and the planned downstream change preview tied to the actionable proposal artifact id. Keep the existing approval checkpoint and revision flow intact: approval must still pause before any sync write, and revision must still target a concrete proposal artifact version. Do not add a separate demo read model; extend the shared workflow detail and proposal-version surfaces so API and Telegram stay aligned.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_status_service.py tests/unit/test_telegram_commands.py tests/integration/test_workflow_status_routes.py -k 'proposal or approval or revision'` passes.</verify>
  <done>Operators can inspect, approve, reject, or request revision on a representative weekly proposal from the shared API and Telegram surfaces without any divergence from kernel approval semantics.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_status_service.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_telegram_commands.py`
- [ ] `uv run --frozen --extra dev pytest tests/integration/test_workflow_status_routes.py`
- [ ] Telegram start creates `weekly_scheduling` runs with a shared durable weekly-request contract rather than the old `weekly_digest` path.
- [ ] API create and Telegram create persist the same representative weekly-request structure and first-step semantics.
- [ ] Normalized task artifacts and schedule proposals are derived from persisted request content, not placeholder constants.
- [ ] Proposal output visibly communicates assumptions and carry-forward work without obscuring the exact scheduled blocks that will drive downstream writes.
- [ ] Approval still pauses before any downstream sync execution, and revision still regenerates a new proposal version for the same run.
- [ ] API and Telegram representative views remain thin wrappers around the shared workflow status projection.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The representative weekly workflow is real enough to start, normalize, propose, pause, and revise using the shared kernel
- Telegram remains the primary operator surface while Postgres-backed artifacts remain the source of truth
- The phase leaves no demo-only path that would bypass approval, lineage, or shared status semantics
  </success_criteria>

<output>
After completion, create `.planning/phases/04-representative-scheduling-workflow/04-01-SUMMARY.md`
</output>
