---
phase: 04-representative-scheduling-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - apps/api/src/helm_api/schemas.py
  - apps/api/src/helm_api/services/workflow_status_service.py
  - apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py
  - docs/runbooks/workflow-runs.md
  - packages/orchestration/src/helm_orchestration/schemas.py
  - packages/orchestration/src/helm_orchestration/workflow_service.py
  - tests/unit/test_replay_service.py
  - tests/unit/test_telegram_commands.py
  - tests/unit/test_workflow_orchestration_service.py
  - tests/unit/test_workflow_status_service.py
  - tests/integration/test_workflow_status_routes.py
autonomous: true
requirements:
  - DEMO-01
  - DEMO-04
  - DEMO-05
  - DEMO-06
user_setup: []
must_haves:
  truths:
    - "A completed representative run is not done until the final summary artifact links raw request, normalized and proposal artifacts, the concrete approval decision, and downstream sync lineage."
    - "Completion and recovery summaries must be built from persisted approval and sync records so restart, retry, and replay paths stay inspectable and truthful."
    - "Operator-facing completion messaging remains Telegram-first and outcome-first, but detailed lineage stays available through the shared API and workflow detail surfaces."
    - "Verification must prove more than a happy path by covering revision, approval gating, sync execution, and restart-safe resume during representative workflow execution."
  artifacts:
    - path: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      provides: "Representative final-summary assembly populated from durable approval and sync lineage."
    - path: "apps/api/src/helm_api/services/workflow_status_service.py"
      provides: "Shared completion and lineage projection for representative workflow detail and operator summaries."
    - path: "apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py"
      provides: "Compact Telegram completion summaries that foreground scheduled outcome, sync results, and carry-forward work."
    - path: "docs/runbooks/workflow-runs.md"
      provides: "Manual verification notes for create, approval, revision, restart-safe resume, and completed representative-flow inspection."
    - path: "tests/unit/test_workflow_orchestration_service.py"
      provides: "Coverage for final summary linkage, representative recovery behavior, and version-specific approval lineage through completion."
  key_links:
    - from: "packages/orchestration/src/helm_orchestration/workflow_service.py"
      to: "packages/orchestration/src/helm_orchestration/schemas.py"
      via: "Final summary payloads populate approval decision and downstream sync linkage using the typed summary artifact contract."
    - from: "apps/api/src/helm_api/services/workflow_status_service.py"
      to: "apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py"
      via: "Telegram completion output is derived from the shared representative status projection instead of bot-only formatting logic."
    - from: "tests/unit/test_workflow_orchestration_service.py"
      to: "tests/integration/test_workflow_status_routes.py"
      via: "Unit and integration tests together prove representative lineage and completion details survive end-to-end from kernel state to operator surface."
---

<objective>
Complete representative-flow lineage, completion summaries, and end-to-end verification.

Purpose: Make Phase 4 prove the kernel all the way through completion by populating a truthful final summary artifact, surfacing operator-usable completion status, and verifying representative create, revision, approval, sync, and recovery behavior across the shared API, worker, and Telegram paths.
Output: Final summary linkage populated from durable artifacts and sync rows, outcome-first completion summaries, manual verification notes, and a representative validation suite that exercises restart and revision paths.
</objective>

<execution_context>
@/Users/ankush/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ankush/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-representative-scheduling-workflow/04-CONTEXT.md
@.planning/phases/04-representative-scheduling-workflow/04-RESEARCH.md
@.planning/phases/04-representative-scheduling-workflow/04-VALIDATION.md
@.planning/phases/04-representative-scheduling-workflow/04-01-PLAN.md
@docs/internal/helm-v1.md
@packages/orchestration/src/helm_orchestration/schemas.py
@packages/orchestration/src/helm_orchestration/workflow_service.py
@apps/api/src/helm_api/services/workflow_status_service.py
@apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Populate representative final-summary artifacts from approval and sync lineage</name>
  <files>packages/orchestration/src/helm_orchestration/schemas.py, packages/orchestration/src/helm_orchestration/workflow_service.py, tests/unit/test_workflow_orchestration_service.py</files>
  <action>Extend final-summary assembly so a completed `weekly_scheduling` run records the approved proposal version, approval decision artifact id, downstream sync status, and downstream sync record or reference identifiers in the typed summary payload instead of leaving those fields null. Build the summary from persisted artifacts, approval checkpoints, and sync rows already owned by the kernel. Make the automated scenario prove both task-system and calendar-system write paths execute after approval and that the persisted sync lineage referenced by the final summary matches those writes. Keep this boring and durable: do not reconstruct lineage from Telegram text or free-form event summaries, and do not change the approval-before-write rule.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py -k 'final_summary or summary or apply_schedule'` passes.</verify>
  <done>The representative workflow can complete with a final summary artifact that actually proves lineage from request through approved write execution.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten representative completion and recovery summaries on shared operator surfaces</name>
  <files>apps/api/src/helm_api/schemas.py, apps/api/src/helm_api/services/workflow_status_service.py, apps/telegram-bot/src/helm_telegram_bot/commands/workflows.py, tests/unit/test_workflow_status_service.py, tests/unit/test_telegram_commands.py, tests/integration/test_workflow_status_routes.py</files>
  <action>Update the shared API and Telegram read models so completed representative runs foreground what was scheduled, what synced, and what still needs attention, while still exposing version history and deeper lineage on demand. Make the completion summary compact and outcome-first rather than dumping every block or sync row inline. Ensure recovery-sensitive states remain truthful during restart or replay-adjacent scenarios by projecting from persisted sync status and the completed final summary artifact instead of stale event text.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_status_service.py tests/unit/test_telegram_commands.py tests/integration/test_workflow_status_routes.py -k 'summary or completion or lineage'` passes.</verify>
  <done>Representative completion views are useful to an operator by default and still remain grounded in the shared durable status projection.</done>
</task>

<task type="auto">
  <name>Task 3: Add representative end-to-end and recovery verification plus runbook notes</name>
  <files>docs/runbooks/workflow-runs.md, tests/unit/test_replay_service.py, tests/unit/test_workflow_orchestration_service.py, tests/unit/test_workflow_status_service.py, tests/integration/test_workflow_status_routes.py</files>
  <action>Add hermetic verification for the representative scenarios defined in `04-VALIDATION.md`: create a weekly request with multiple tasks and explicit constraints, prove API and Telegram create paths persist the same `weekly_scheduling` request contract, pause for approval before any sync write, request revision with natural-language feedback to generate a new proposal version, approve and execute both task and calendar downstream writes, and confirm the completed run exposes request-to-sync lineage through the persisted sync rows referenced by the final summary. Add restart-safe coverage for representative execution around approval wait and `apply_schedule`, plus concise runbook steps for manual Telegram/API verification of request UX, proposal honesty, and completion summary clarity.</action>
  <verify>`uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py tests/unit/test_workflow_status_service.py tests/unit/test_replay_service.py tests/integration/test_workflow_status_routes.py` passes.</verify>
  <done>The representative flow is proven beyond the happy path, and the runbook tells an operator exactly how to verify the shipped workflow manually.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_orchestration_service.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_workflow_status_service.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_telegram_commands.py`
- [ ] `uv run --frozen --extra dev pytest tests/unit/test_replay_service.py`
- [ ] `uv run --frozen --extra dev pytest tests/integration/test_workflow_status_routes.py`
- [ ] Final summary artifacts populate approval decision linkage and downstream sync linkage for representative completed runs.
- [ ] Automated representative-flow coverage proves both task and calendar approved-write paths execute and persist sync lineage that the final summary references.
- [ ] Completion summaries show scheduled outcome, sync counts or status, and carry-forward attention items without hiding the inspectable lineage.
- [ ] Representative restart and recovery tests prove state survives approval wait and approved-write execution boundaries.
- [ ] Manual verification notes cover Telegram start, proposal revision, approval, and completed-run inspection for the representative workflow.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The representative workflow finishes with a truthful final summary artifact and operator-visible completion summary
- Lineage remains durable from raw request through approval decision and downstream sync results
- The phase ships with automated and manual verification strong enough to prove the kernel, not just a happy-path demo
  </success_criteria>

<output>
After completion, create `.planning/phases/04-representative-scheduling-workflow/04-02-SUMMARY.md`
</output>
